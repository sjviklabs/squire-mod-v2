---
phase: 02-brain-fsm-follow
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/com/sjviklabs/squire/brain/SquireAIState.java
  - src/main/java/com/sjviklabs/squire/brain/AITransition.java
  - src/main/java/com/sjviklabs/squire/brain/TickRateStateMachine.java
  - src/main/java/com/sjviklabs/squire/entity/SquireEntity.java
autonomous: true
requirements: [ARC-01, NAV-05]

must_haves:
  truths:
    - "TickRateStateMachine ticks each server aiStep without crash"
    - "AITransition global (null sourceState) fires from any FSM state"
    - "Squire opens doors and floats in water (registerGoals wired)"
    - "All 27 SquireAIState values are defined — no future enum changes needed"
  artifacts:
    - path: "src/main/java/com/sjviklabs/squire/brain/SquireAIState.java"
      provides: "Full 27-state enum used by all Phase 2+ FSM transitions"
      contains: "IDLE, EATING, FLEEING, COMBAT_APPROACH, FOLLOWING_OWNER, SITTING"
    - path: "src/main/java/com/sjviklabs/squire/brain/AITransition.java"
      provides: "Record: sourceState, condition, action, tickRate, priority"
      contains: "record AITransition"
    - path: "src/main/java/com/sjviklabs/squire/brain/TickRateStateMachine.java"
      provides: "Per-transition countdown engine, global+state transition evaluation"
      contains: "public void tick(SquireEntity"
  key_links:
    - from: "SquireEntity.aiStep()"
      to: "SquireBrain.tick()"
      via: "lazy init guard already in Phase 1 code"
      pattern: "squireBrain\\.tick\\(\\)"
    - from: "SquireEntity.registerGoals()"
      to: "FloatGoal + OpenDoorGoal"
      via: "goalSelector.addGoal"
      pattern: "OpenDoorGoal"
---

<objective>
Port the TickRateStateMachine engine verbatim from v0.5.0, define the full SquireAIState enum (all 27 states — never changes again), and wire the minimal SquireEntity.registerGoals() that enables door-opening and water floating. This is the pure foundation layer — no behavior logic yet.

Purpose: Every Phase 2+ plan depends on AITransition, SquireAIState, and TickRateStateMachine existing. Build them first as a standalone wave.
Output: 3 new brain/ classes + SquireEntity.registerGoals() updated.
</objective>

<execution_context>
@C:/Users/Steve/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Steve/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/Steve/Projects/squire-mod-v2/.planning/PROJECT.md
@C:/Users/Steve/Projects/squire-mod-v2/.planning/ROADMAP.md
@C:/Users/Steve/Projects/squire-mod-v2/.planning/phases/02-brain-fsm-follow/02-CONTEXT.md
@C:/Users/Steve/Projects/squire-mod-v2/.planning/phases/02-brain-fsm-follow/02-RESEARCH.md

<interfaces>
<!-- Existing Phase 1 contracts the executor must match -->

From src/main/java/com/sjviklabs/squire/entity/SquireEntity.java (relevant excerpts):
```java
// SynchedEntityData modes (already defined):
public static final byte MODE_FOLLOW = 0;
public static final byte MODE_SIT    = 1;
public static final byte MODE_GUARD  = 2;

// Lazy brain init in aiStep() — already wired:
private SquireBrain squireBrain;

@Override
public void aiStep() {
    if (!this.level().isClientSide && this.squireBrain == null) {
        this.squireBrain = new SquireBrain(this);
    }
    if (this.squireBrain != null) {
        this.squireBrain.tick();
    }
    super.aiStep();
}

// registerGoals() currently has FloatGoal(0) and OpenDoorGoal(1) from Phase 1-02
// — verify before adding; do not duplicate
```

From src/main/java/com/sjviklabs/squire/brain/SquireBrain.java (stub to expand later):
```java
public class SquireBrain {
    private final SquireEntity squire;
    public SquireBrain(SquireEntity squire) { this.squire = squire; }
    public void tick() { /* Phase 2 */ }
}
```

v0.5.0 AITransition record signature (port verbatim):
```java
public record AITransition(
    SquireAIState sourceState,    // null = global (fires from any state)
    BooleanSupplier condition,
    Function<SquireEntity, SquireAIState> action,
    int tickRate,
    int priority
) {
    public boolean isGlobal() { return sourceState == null; }
    public boolean appliesTo(SquireAIState state) { return sourceState == state; }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SquireAIState enum + AITransition record</name>
  <files>
    src/main/java/com/sjviklabs/squire/brain/SquireAIState.java
    src/main/java/com/sjviklabs/squire/brain/AITransition.java
  </files>
  <read_first>
    - C:/Users/Steve/Projects/squire-mod-v2/src/main/java/com/sjviklabs/squire/brain/SquireBrain.java (existing stub — package name)
    - C:/Users/Steve/Projects/squire-mod/src/main/java/com/sjviklabs/squire/ai/statemachine/SquireAIState.java (v0.5.0 reference — copy state list verbatim)
    - C:/Users/Steve/Projects/squire-mod/src/main/java/com/sjviklabs/squire/ai/statemachine/AITransition.java (v0.5.0 reference — copy record verbatim)
  </read_first>
  <action>
    Create SquireAIState.java in package com.sjviklabs.squire.brain.

    Copy ALL 27 states from v0.5.0 verbatim:
    IDLE, EATING, FLEEING,
    COMBAT_APPROACH, COMBAT_ATTACK, COMBAT_RANGED,
    MOUNTING, MOUNTED_IDLE, MOUNTED_FOLLOW, MOUNTED_COMBAT,
    FOLLOWING_OWNER, SITTING,
    MINING_APPROACH, MINING_BREAK,
    PLACING_APPROACH, PLACING_BLOCK, PICKING_UP_ITEM,
    CHEST_APPROACH, CHEST_INTERACT,
    PATROL_WALK, PATROL_WAIT,
    FARM_APPROACH, FARM_WORK, FARM_SCAN,
    FISHING_APPROACH, FISHING_IDLE,
    LOOKING_AROUND, WANDERING

    Create AITransition.java in package com.sjviklabs.squire.brain.

    Copy the record verbatim from v0.5.0:
    - Fields: sourceState (SquireAIState, nullable), condition (BooleanSupplier),
      action (Function&lt;SquireEntity, SquireAIState&gt;), tickRate (int), priority (int)
    - Helpers: isGlobal(), appliesTo(SquireAIState)
    - Import: com.sjviklabs.squire.entity.SquireEntity (not v0.5.0 path)

    Do NOT import or reference any v0.5.0 class paths. New package is com.sjviklabs.squire.brain.
  </action>
  <verify>
    <automated>cd C:/Users/Steve/Projects/squire-mod-v2 && ./gradlew compileJava --no-daemon 2>&1 | grep -E "error:|SquireAIState|AITransition" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - SquireAIState.java exists with exactly 27 enum constants (grep -c "," returns 26 commas or count manually)
    - AITransition.java is a record with 5 fields matching the v0.5.0 signature
    - `./gradlew compileJava` produces zero errors in these two files
    - grep "FOLLOWING_OWNER" src/main/java/com/sjviklabs/squire/brain/SquireAIState.java returns a match
    - grep "SITTING" src/main/java/com/sjviklabs/squire/brain/SquireAIState.java returns a match
  </acceptance_criteria>
  <done>Both files compile clean; enum has all 27 states; AITransition is a record with nullable sourceState and Function&lt;SquireEntity, SquireAIState&gt; action.</done>
</task>

<task type="auto">
  <name>Task 2: TickRateStateMachine + registerGoals() wiring</name>
  <files>
    src/main/java/com/sjviklabs/squire/brain/TickRateStateMachine.java
    src/main/java/com/sjviklabs/squire/entity/SquireEntity.java
  </files>
  <read_first>
    - C:/Users/Steve/Projects/squire-mod/src/main/java/com/sjviklabs/squire/ai/statemachine/TickRateStateMachine.java (v0.5.0 — port verbatim)
    - C:/Users/Steve/Projects/squire-mod-v2/src/main/java/com/sjviklabs/squire/entity/SquireEntity.java (check existing registerGoals() — do not duplicate FloatGoal or OpenDoorGoal if already present)
  </read_first>
  <action>
    Create TickRateStateMachine.java in package com.sjviklabs.squire.brain.

    Port verbatim from v0.5.0 with these two changes only:
    1. Update package to com.sjviklabs.squire.brain
    2. Update imports to use com.sjviklabs.squire.brain.AITransition and SquireAIState
       (not v0.5.0 paths)

    Do NOT change the algorithm. Keep: dirty flag + rebuild(), countdowns int[],
    globalIndices int[], tick() evaluation order (globals by priority first,
    then current-state transitions).

    Update SquireEntity.registerGoals():
    Read the existing method body first. Add only what is missing — do not duplicate.
    Required goals if not present:
      this.goalSelector.addGoal(0, new FloatGoal(this));
      this.goalSelector.addGoal(1, new OpenDoorGoal(this, true));
      this.targetSelector.addGoal(1, new OwnerHurtByTargetGoal(this));

    Also add to SquireEntity constructor or initEntityProperties():
      this.setCanPickUpLoot(false);
      navigation.setCanFloat(true);  // NAV-03 water traversal baseline

    Add these helper methods to SquireEntity if not already present:
      public boolean isOrderedToSit() { return getSquireMode() == MODE_SIT; }
      public boolean shouldFollowOwner() { return getSquireMode() == MODE_FOLLOW; }
      public void setSquireSprinting(boolean sprint) { this.setSprinting(sprint); }
      public @Nullable Player getOwner() {
          if (ownerUUID == null) return null;
          return level().getPlayerByUUID(ownerUUID);
      }

    Import: net.minecraft.world.entity.ai.goal.FloatGoal,
            net.minecraft.world.entity.ai.goal.OpenDoorGoal,
            net.minecraft.world.entity.ai.goal.target.OwnerHurtByTargetGoal
            (verify these exist in NeoForge 1.21.1 — OwnerHurtByTargetGoal is in
             net.minecraft.world.entity.ai.goal.target package)
  </action>
  <verify>
    <automated>cd C:/Users/Steve/Projects/squire-mod-v2 && ./gradlew compileJava --no-daemon 2>&1 | grep -E "error:|warning:" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - TickRateStateMachine.java exists in brain package with tick(SquireEntity) method
    - grep "dirty" src/main/java/com/sjviklabs/squire/brain/TickRateStateMachine.java returns a match (dirty flag preserved)
    - grep "OpenDoorGoal" src/main/java/com/sjviklabs/squire/entity/SquireEntity.java returns a match
    - grep "isOrderedToSit" src/main/java/com/sjviklabs/squire/entity/SquireEntity.java returns a match
    - grep "shouldFollowOwner" src/main/java/com/sjviklabs/squire/entity/SquireEntity.java returns a match
    - grep "getOwner" src/main/java/com/sjviklabs/squire/entity/SquireEntity.java returns a match
    - ./gradlew compileJava exits 0 with zero errors
  </acceptance_criteria>
  <done>TickRateStateMachine compiles and integrates into the brain package; SquireEntity has the four helper methods Phase 2 handlers require; registerGoals has FloatGoal + OpenDoorGoal.</done>
</task>

</tasks>

<verification>
After both tasks:
- `./gradlew compileJava --no-daemon` exits 0
- `find src/main/java/com/sjviklabs/squire/brain -name "*.java" | wc -l` returns at least 4 (SquireAIState, AITransition, TickRateStateMachine, SquireBrain stub)
- grep -r "SquireAIState.SITTING" src/ returns at least one result
- grep -r "setCanFloat" src/ returns at least one result
</verification>

<success_criteria>
- SquireAIState enum has all 27 states — no future plans need to add states
- AITransition is a record with nullable sourceState supporting global transitions
- TickRateStateMachine tick() evaluates globals first, current-state second, by priority
- SquireEntity has isOrderedToSit(), shouldFollowOwner(), setSquireSprinting(), getOwner()
- Full compile passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-brain-fsm-follow/02-01-SUMMARY.md` following the summary template.
</output>
