---
phase: 02-brain-fsm-follow
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/main/java/com/sjviklabs/squire/brain/SquireEvent.java
  - src/main/java/com/sjviklabs/squire/brain/SquireBrainEventBus.java
  - src/main/java/com/sjviklabs/squire/brain/SquireBrain.java
autonomous: true
requirements: [ARC-02, ARC-03]

must_haves:
  truths:
    - "SquireBrainEventBus dispatches to all subscribers for a given event"
    - "Subscribers registered BEFORE registerTransitions() is called — no silent event drops"
    - "SquireBrain owns the machine, event bus, and handler stubs — Phase 4+ handlers bolt on without changing SquireBrain constructor"
    - "Handler-per-behavior pattern is established: each handler is a separate class owning its mutable state"
  artifacts:
    - path: "src/main/java/com/sjviklabs/squire/brain/SquireEvent.java"
      provides: "Enum of 3 events: SIT_TOGGLE, COMBAT_START, COMBAT_END"
      contains: "SIT_TOGGLE"
    - path: "src/main/java/com/sjviklabs/squire/brain/SquireBrainEventBus.java"
      provides: "EnumMap-backed subscribe/publish bus, instance-scoped to one squire"
      contains: "EnumMap"
    - path: "src/main/java/com/sjviklabs/squire/brain/SquireBrain.java"
      provides: "TickRateStateMachine + SquireBrainEventBus container, tick() entry point"
      contains: "TickRateStateMachine"
  key_links:
    - from: "SquireBrain constructor"
      to: "bus.subscribe() calls"
      via: "called before registerTransitions()"
      pattern: "bus\\.subscribe"
    - from: "SquireBrain.tick()"
      to: "machine.tick(squire)"
      via: "direct delegation"
      pattern: "machine\\.tick"
---

<objective>
Build SquireBrainEventBus (new in v2 — not in v0.5.0), define the SquireEvent enum, and expand SquireBrain from its Phase 1 stub into the real container that owns the machine, bus, and handler references. Phase 2 only needs SITTING state — FollowHandler and SurvivalHandler slot in via Plans 02-03 and 02-04 respectively.

Purpose: Eliminates direct handler-to-handler coupling. ARC-03 compliance. Phase 4 CombatHandler publishes COMBAT_START/END without knowing who subscribes.
Output: SquireEvent, SquireBrainEventBus, SquireBrain (expanded).
</objective>

<execution_context>
@C:/Users/Steve/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Steve/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/Steve/Projects/squire-mod-v2/.planning/phases/02-brain-fsm-follow/02-CONTEXT.md
@C:/Users/Steve/Projects/squire-mod-v2/.planning/phases/02-brain-fsm-follow/02-RESEARCH.md
@C:/Users/Steve/Projects/squire-mod-v2/.planning/phases/02-brain-fsm-follow/02-01-SUMMARY.md

<interfaces>
<!-- Produced by Plan 02-01 — executor must use these exact signatures -->

From src/main/java/com/sjviklabs/squire/brain/TickRateStateMachine.java:
```java
public class TickRateStateMachine {
    public void addTransition(AITransition transition) { ... }
    public SquireAIState getCurrentState() { ... }
    public void tick(SquireEntity squire) { ... }
}
```

From src/main/java/com/sjviklabs/squire/brain/AITransition.java:
```java
public record AITransition(
    SquireAIState sourceState,   // null = global
    BooleanSupplier condition,
    Function<SquireEntity, SquireAIState> action,
    int tickRate,
    int priority
) {}
```

From src/main/java/com/sjviklabs/squire/entity/SquireEntity.java (Phase 1 + 02-01 additions):
```java
public boolean isOrderedToSit()       // getSquireMode() == MODE_SIT
public boolean shouldFollowOwner()    // getSquireMode() == MODE_FOLLOW
public void setSquireSprinting(bool)  // delegates to setSprinting()
public @Nullable Player getOwner()    // looks up ownerUUID in level
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SquireEvent enum + SquireBrainEventBus</name>
  <files>
    src/main/java/com/sjviklabs/squire/brain/SquireEvent.java
    src/main/java/com/sjviklabs/squire/brain/SquireBrainEventBus.java
  </files>
  <read_first>
    - C:/Users/Steve/Projects/squire-mod-v2/.planning/phases/02-brain-fsm-follow/02-RESEARCH.md (Pattern 3: SquireBrainEventBus full impl — lines ~183-215)
    - C:/Users/Steve/Projects/squire-mod-v2/src/main/java/com/sjviklabs/squire/brain/SquireBrain.java (package name)
  </read_first>
  <action>
    Create SquireEvent.java in package com.sjviklabs.squire.brain:
    ```java
    public enum SquireEvent {
        SIT_TOGGLE,   // published when squire enters or exits SITTING state
        COMBAT_START, // published when CombatHandler enters combat (Phase 4 populates subscribers)
        COMBAT_END    // published when CombatHandler exits combat (Phase 4 populates subscribers)
    }
    ```

    Create SquireBrainEventBus.java in package com.sjviklabs.squire.brain.
    Implementation exactly as in RESEARCH.md Pattern 3:
    - Field: `private final Map&lt;SquireEvent, List&lt;Consumer&lt;SquireEntity&gt;&gt;&gt; listeners = new EnumMap&lt;&gt;(SquireEvent.class)`
    - Method subscribe(SquireEvent, Consumer&lt;SquireEntity&gt;): listeners.computeIfAbsent(event, k -> new ArrayList&lt;&gt;()).add(handler)
    - Method publish(SquireEvent, SquireEntity): get list, null-check, forEach(h -> h.accept(squire))
    - This is NOT NeoForge's IEventBus — it is a plain Java class, instance-scoped to one squire.
    - No synchronization needed (single-threaded server tick).

    Imports for SquireBrainEventBus:
      java.util.ArrayList, java.util.EnumMap, java.util.List, java.util.Map, java.util.function.Consumer
      com.sjviklabs.squire.entity.SquireEntity
  </action>
  <verify>
    <automated>cd C:/Users/Steve/Projects/squire-mod-v2 && ./gradlew compileJava --no-daemon 2>&1 | grep -E "error:|SquireEvent|SquireBrainEventBus" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "SIT_TOGGLE" src/main/java/com/sjviklabs/squire/brain/SquireEvent.java returns a match
    - grep "COMBAT_START" src/main/java/com/sjviklabs/squire/brain/SquireEvent.java returns a match
    - grep "EnumMap" src/main/java/com/sjviklabs/squire/brain/SquireBrainEventBus.java returns a match
    - grep "publish" src/main/java/com/sjviklabs/squire/brain/SquireBrainEventBus.java returns a match
    - grep "subscribe" src/main/java/com/sjviklabs/squire/brain/SquireBrainEventBus.java returns a match
    - ./gradlew compileJava exits 0
  </acceptance_criteria>
  <done>SquireEvent has 3 events; SquireBrainEventBus compiles as a plain Java subscriber/publisher with EnumMap backing.</done>
</task>

<task type="auto">
  <name>Task 2: SquireBrain — real container replacing Phase 1 stub</name>
  <files>
    src/main/java/com/sjviklabs/squire/brain/SquireBrain.java
  </files>
  <read_first>
    - C:/Users/Steve/Projects/squire-mod-v2/src/main/java/com/sjviklabs/squire/brain/SquireBrain.java (Phase 1 stub — replace fully)
    - C:/Users/Steve/Projects/squire-mod-v2/.planning/phases/02-brain-fsm-follow/02-RESEARCH.md (Pattern 2: SquireBrain minimal Phase 2 impl — lines ~133-170)
    - C:/Users/Steve/Projects/squire-mod/src/main/java/com/sjviklabs/squire/ai/statemachine/SquireAI.java (v0.5.0 registerTransitions() pattern)
  </read_first>
  <action>
    Replace SquireBrain.java completely. New implementation:

    Fields:
      private final SquireEntity squire
      private final TickRateStateMachine machine
      private final SquireBrainEventBus bus
      // FollowHandler and SurvivalHandler are null stubs here — Plans 02-03 and 02-04 add them.
      // Declare as Object placeholders with TODO comments OR leave them out and let 02-03/02-04 add them.
      // PREFERRED: Add the field declarations now as null, let later plans initialize.
      //   private FollowHandler follow = null;
      //   private SurvivalHandler survival = null;
      // This avoids SquireBrain.java being modified in 3 consecutive plans.
      private int idleTicks

    Constructor order (CRITICAL — subscribe before registerTransitions):
      1. this.squire = squire
      2. this.machine = new TickRateStateMachine()
      3. this.bus = new SquireBrainEventBus()
      4. [bus subscriptions go here — before any transitions can fire]
      5. registerTransitions()  // only SITTING transitions in Phase 2

    registerTransitions() — Phase 2 only implements SITTING entry/exit:

    Enter SITTING (global, priority 1, tickRate 1):
      condition: machine.getCurrentState() != SquireAIState.SITTING
                 && squire.isOrderedToSit()
                 && !squire.isInWaterOrBubble()
      action: s -> {
          s.getNavigation().stop();
          s.setInSittingPose(true);
          bus.publish(SquireEvent.SIT_TOGGLE, s);
          return SquireAIState.SITTING;
      }

    Exit SITTING (sourceState=SITTING, priority 1, tickRate 1):
      condition: !squire.isOrderedToSit()
      action: s -> {
          s.setInSittingPose(false);
          bus.publish(SquireEvent.SIT_TOGGLE, s);
          return SquireAIState.IDLE;
      }

    tick() method:
      if (machine.getCurrentState() != SquireAIState.IDLE) idleTicks = 0;
      machine.tick(squire);

    Important: setInSittingPose(boolean) is on LivingEntity in NeoForge 1.21.1.
    Verify via: grep -r "setInSittingPose" in NeoForge sources or use setPose(Pose.SITTING) if unavailable.
    Fallback if setInSittingPose is absent: call squire.setPose(net.minecraft.world.entity.Pose.SITTING)
    and squire.setPose(net.minecraft.world.entity.Pose.STANDING) for exit.

    Add getter: public TickRateStateMachine getMachine() { return machine; }
    Add getter: public SquireBrainEventBus getBus() { return bus; }

    Leave FollowHandler and SurvivalHandler fields as:
      // Set by Phase 2 plans 02-03 / 02-04 post-construction
      FollowHandler follow;      // package-private so handlers can set themselves
      SurvivalHandler survival;  // package-private
    Do NOT import these yet if their classes don't exist — use forward declaration pattern or
    declare as Object and cast later (same pattern as SquireEntity.itemHandler in Phase 1).
    SIMPLEST: just add TODO comments and let plans 02-03/02-04 add the fields when they create the classes.
  </action>
  <verify>
    <automated>cd C:/Users/Steve/Projects/squire-mod-v2 && ./gradlew compileJava --no-daemon 2>&1 | grep -E "error:|SquireBrain" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "TickRateStateMachine" src/main/java/com/sjviklabs/squire/brain/SquireBrain.java returns a match
    - grep "SquireBrainEventBus" src/main/java/com/sjviklabs/squire/brain/SquireBrain.java returns a match
    - grep "SIT_TOGGLE" src/main/java/com/sjviklabs/squire/brain/SquireBrain.java returns a match (transition fires event)
    - grep "registerTransitions" src/main/java/com/sjviklabs/squire/brain/SquireBrain.java returns a match
    - grep "subscribe" src/main/java/com/sjviklabs/squire/brain/SquireBrain.java returns zero or more matches (subscriptions set up before registerTransitions)
    - ./gradlew compileJava exits 0
  </acceptance_criteria>
  <done>SquireBrain is a real FSM container with SITTING entry/exit transitions; event bus wired; tick() delegates to machine.tick(). Full project compiles clean.</done>
</task>

</tasks>

<verification>
After both tasks:
- `./gradlew compileJava --no-daemon` exits 0
- `find src/main/java/com/sjviklabs/squire/brain -name "*.java" | sort` lists SquireAIState, AITransition, TickRateStateMachine, SquireBrain, SquireEvent, SquireBrainEventBus (6 files)
- grep -r "SIT_TOGGLE" src/ returns at least 2 matches (SquireEvent definition + SquireBrain publish)
- grep -r "NeoForge.EVENT_BUS\|MinecraftForge.EVENT_BUS" src/main/java/com/sjviklabs/squire/brain/ returns 0 (no global bus leakage)
</verification>

<success_criteria>
- SquireBrainEventBus is instance-scoped (EnumMap), not the NeoForge global bus
- Subscriptions are registered in SquireBrain constructor BEFORE registerTransitions() is called
- SITTING state entry/exit transitions are registered with priority 1, tickRate 1
- Both SITTING transitions publish SIT_TOGGLE to the bus
- getMachine() and getBus() accessors exist for later plans to use
</success_criteria>

<output>
After completion, create `.planning/phases/02-brain-fsm-follow/02-02-SUMMARY.md` following the summary template.
</output>
