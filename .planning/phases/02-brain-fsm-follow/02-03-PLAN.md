---
phase: 02-brain-fsm-follow
plan: "03"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/main/java/com/sjviklabs/squire/brain/handler/FollowHandler.java
  - src/main/java/com/sjviklabs/squire/brain/SquireBrain.java
autonomous: true
requirements: [NAV-01, NAV-02, NAV-03, NAV-05]

must_haves:
  truths:
    - "Squire paths toward the player when owner is farther than followStartDistance — no teleportation ever"
    - "Squire sprints when distSq > sprintDistance^2 OR when owner is sprinting"
    - "Squire floats and swims through rivers — does not stop at water edge"
    - "When stuck for 3 seconds, squire gets jump boost and replans path — does not freeze"
    - "No tryTeleportToOwner method exists anywhere in FollowHandler — not disabled, deleted"
  artifacts:
    - path: "src/main/java/com/sjviklabs/squire/brain/handler/FollowHandler.java"
      provides: "No-teleport follow with sprint scaling, stuck detection, door nav"
      contains: "STUCK_THRESHOLD"
    - path: "src/main/java/com/sjviklabs/squire/brain/SquireBrain.java"
      provides: "FOLLOWING_OWNER and IDLE<->FOLLOW transitions registered; FollowHandler wired"
      contains: "FOLLOWING_OWNER"
  key_links:
    - from: "SquireBrain.registerTransitions()"
      to: "follow::shouldFollow / follow::shouldStop"
      via: "IDLE→FOLLOWING_OWNER and FOLLOWING_OWNER→IDLE transitions"
      pattern: "shouldFollow|shouldStop"
    - from: "FollowHandler.tick()"
      to: "squire.getNavigation().moveTo(owner, speed)"
      via: "throttled pathRecalcTimer"
      pattern: "moveTo.*owner.*speed"
---

<objective>
Implement FollowHandler — the no-teleport follow behavior. This is the defining behavior of the squire mod. Port from v0.5.0 but DELETE the teleport method entirely and replace with stuck detection + jump boost recovery. Wire FOLLOWING_OWNER transitions into SquireBrain.

Purpose: NAV-01 is the identity of this mod — no teleporting under any circumstances. NAV-02 (sprint), NAV-03 (water), NAV-05 (cave nav) all land here.
Output: FollowHandler class + FOLLOWING_OWNER transitions in SquireBrain.
</objective>

<execution_context>
@C:/Users/Steve/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Steve/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/Steve/Projects/squire-mod-v2/.planning/phases/02-brain-fsm-follow/02-CONTEXT.md
@C:/Users/Steve/Projects/squire-mod-v2/.planning/phases/02-brain-fsm-follow/02-RESEARCH.md
@C:/Users/Steve/Projects/squire-mod-v2/.planning/phases/02-brain-fsm-follow/02-01-SUMMARY.md

<interfaces>
<!-- Produced by Plan 02-01 -->

SquireEntity accessors available after 02-01:
```java
public boolean isOrderedToSit()       // true when MODE_SIT
public boolean shouldFollowOwner()    // true when MODE_FOLLOW
public void setSquireSprinting(boolean sprint)
public @Nullable Player getOwner()    // looks up ownerUUID in level
// navigation.setCanFloat(true) — already set in 02-01 SquireEntity setup
```

TickRateStateMachine (from 02-01):
```java
public void addTransition(AITransition transition)
public SquireAIState getCurrentState()
```

Config values available via SquireConfig (from Phase 1, plan 01-04):
```java
SquireConfig.followStartDistance.get()  // double — default 6.0
SquireConfig.followStopDistance.get()   // double — default 2.0
SquireConfig.sprintDistance.get()       // double — sprint threshold
SquireConfig.pathRecalcInterval.get()   // int — default ~10 ticks
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FollowHandler — no-teleport follow with stuck detection</name>
  <files>
    src/main/java/com/sjviklabs/squire/brain/handler/FollowHandler.java
  </files>
  <read_first>
    - C:/Users/Steve/Projects/squire-mod/src/main/java/com/sjviklabs/squire/ai/handler/FollowHandler.java (v0.5.0 reference — port structure, DELETE teleport)
    - C:/Users/Steve/Projects/squire-mod-v2/.planning/phases/02-brain-fsm-follow/02-RESEARCH.md (Pattern 4: FollowHandler — full stuck detection code, door nav code, speed scaling code)
    - C:/Users/Steve/Projects/squire-mod-v2/src/main/java/com/sjviklabs/squire/config/SquireConfig.java (verify config field names before referencing)
  </read_first>
  <action>
    Create src/main/java/com/sjviklabs/squire/brain/handler/FollowHandler.java
    (create the handler/ subdirectory under brain/).

    Package: com.sjviklabs.squire.brain.handler

    Fields:
      private final SquireEntity squire
      private int pathRecalcTimer = 0
      private Vec3 lastPos = null
      private int stuckTicks = 0
      private static final int STUCK_THRESHOLD = 60  // 3 seconds at 20 TPS

    Methods to implement:

    shouldFollow():
      if (!squire.shouldFollowOwner()) return false
      Player owner = squire.getOwner(); if null or dead or spectator: return false
      double dist = SquireConfig.followStartDistance.get()
      return squire.distanceToSqr(owner) >= dist * dist

    shouldStop():
      if (!squire.shouldFollowOwner()) return true
      Player owner = squire.getOwner(); if null or dead or spectator: return true
      double dist = SquireConfig.followStopDistance.get()
      return squire.distanceToSqr(owner) <= dist * dist

    start():
      pathRecalcTimer = 0; lastPos = null; stuckTicks = 0
      if (squire.getNavigation() instanceof GroundPathNavigation nav):
        nav.setCanOpenDoors(true); nav.setCanPassDoors(true)

    stop():
      squire.setSquireSprinting(false)
      squire.getNavigation().stop()
      if (squire.getNavigation() instanceof GroundPathNavigation nav):
        nav.setCanOpenDoors(false); nav.setCanPassDoors(false)

    tick(SquireEntity s) returns SquireAIState:
      Player owner = s.getOwner(); if null: return SquireAIState.IDLE

      // Head tracking
      s.getLookControl().setLookAt(owner, 10.0F, (float) s.getMaxHeadXRot())

      // Stuck detection — runs every tick regardless of pathRecalcTimer
      Vec3 currentPos = s.position()
      if (lastPos != null && currentPos.distanceTo(lastPos) < 0.1) {
          stuckTicks++
          if (stuckTicks >= STUCK_THRESHOLD) {
              handleStuck(s, owner)
              stuckTicks = 0
          }
      } else {
          stuckTicks = 0
      }
      lastPos = currentPos

      // Throttled path recalculation
      if (--pathRecalcTimer > 0) return SquireAIState.FOLLOWING_OWNER
      pathRecalcTimer = SquireConfig.pathRecalcInterval.get()

      double distSq = s.distanceToSqr(owner)

      // Jump boost for elevation (owner is 1.5+ blocks above, within 20 blocks)
      double heightDiff = owner.getY() - s.getY()
      if (heightDiff > 1.5 && distSq < 20.0 * 20.0) {
          if (!s.hasEffect(MobEffects.JUMP)) {
              s.addEffect(new MobEffectInstance(MobEffects.JUMP, 40, 1, false, false))
          }
      }

      // Sprint scaling
      double sprintThresh = SquireConfig.sprintDistance.get()
      boolean shouldSprint = distSq > sprintThresh * sprintThresh || owner.isSprinting()
      s.setSquireSprinting(shouldSprint)
      double speed = shouldSprint ? 1.3D : 1.0D
      s.getNavigation().moveTo(owner, speed)

      return SquireAIState.FOLLOWING_OWNER

    handleStuck(SquireEntity s, Player owner) private void:
      // Jump boost — helps with stairs, lips, cliffs
      s.addEffect(new MobEffectInstance(MobEffects.JUMP, 40, 2, false, false))
      // Force path replan
      s.getNavigation().stop()
      s.getNavigation().moveTo(owner, 1.3D)
      // Ensure swimming active if in water
      if (s.isInWater()) s.setSwimming(true)

    CRITICAL — DO NOT ADD:
    - tryTeleportToOwner() method — not allowed, not configurable, completely absent
    - Any distance check that returns early when squire is "too far" (e.g., distSq > 24*24 early return)
    - Any call to s.moveTo() or s.teleportTo() for navigation

    Imports needed:
      net.minecraft.world.effect.MobEffectInstance, MobEffects
      net.minecraft.world.entity.ai.navigation.GroundPathNavigation
      net.minecraft.world.entity.player.Player
      net.minecraft.world.phys.Vec3
      com.sjviklabs.squire.brain.SquireAIState
      com.sjviklabs.squire.config.SquireConfig
      com.sjviklabs.squire.entity.SquireEntity
  </action>
  <verify>
    <automated>cd C:/Users/Steve/Projects/squire-mod-v2 && ./gradlew compileJava --no-daemon 2>&1 | grep -E "error:|FollowHandler" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File exists at src/main/java/com/sjviklabs/squire/brain/handler/FollowHandler.java
    - grep "tryTeleportToOwner" src/main/java/com/sjviklabs/squire/brain/handler/FollowHandler.java returns 0 matches (method does not exist)
    - grep "STUCK_THRESHOLD" src/main/java/com/sjviklabs/squire/brain/handler/FollowHandler.java returns a match
    - grep "handleStuck" src/main/java/com/sjviklabs/squire/brain/handler/FollowHandler.java returns at least 2 matches (declaration + call)
    - grep "moveTo.*owner" src/main/java/com/sjviklabs/squire/brain/handler/FollowHandler.java returns at least 1 match (navigation, not teleport)
    - grep "setCanOpenDoors" src/main/java/com/sjviklabs/squire/brain/handler/FollowHandler.java returns a match (start())
    - ./gradlew compileJava exits 0
  </acceptance_criteria>
  <done>FollowHandler compiles; no teleport method; stuck detection with jump boost recovery; sprint scaling from config; door navigation on enter/exit.</done>
</task>

<task type="auto">
  <name>Task 2: Wire FOLLOWING_OWNER transitions into SquireBrain</name>
  <files>
    src/main/java/com/sjviklabs/squire/brain/SquireBrain.java
  </files>
  <read_first>
    - C:/Users/Steve/Projects/squire-mod-v2/src/main/java/com/sjviklabs/squire/brain/SquireBrain.java (current state after Plan 02-02)
    - C:/Users/Steve/Projects/squire-mod-v2/.planning/phases/02-brain-fsm-follow/02-RESEARCH.md (Pattern: registerFollowTransitions code block ~lines 505-530)
    - C:/Users/Steve/Projects/squire-mod-v2/.planning/phases/02-brain-fsm-follow/02-02-SUMMARY.md (what was built — check FollowHandler field declaration)
  </read_first>
  <action>
    Update SquireBrain.java to:

    1. Add FollowHandler field (if Plan 02-02 left it as Object placeholder, replace with proper type):
       private final FollowHandler follow;

    2. Initialize in constructor BEFORE registerTransitions():
       this.follow = new FollowHandler(squire);
       (Note: FollowHandler does not need the bus in Phase 2 — no bus parameter)

    3. Add registerFollowTransitions() method and call it from registerTransitions():

    IDLE → FOLLOWING_OWNER (sourceState=IDLE, priority 30, tickRate 10):
      condition: follow::shouldFollow
      action: s -> { follow.start(); return SquireAIState.FOLLOWING_OWNER; }

    FOLLOWING_OWNER → IDLE (sourceState=FOLLOWING_OWNER, priority 30, tickRate 10):
      condition: follow::shouldStop
      action: s -> { follow.stop(); return SquireAIState.IDLE; }

    FOLLOWING_OWNER tick (sourceState=FOLLOWING_OWNER, priority 31, tickRate 1):
      condition: () -> true  // always runs when in this state
      action: s -> follow.tick(s)

    Priority 31 for the per-tick follow action ensures it is evaluated after the stop
    check (priority 30) — machine evaluates state transitions by priority ascending;
    lower number = higher priority. So STOP (30) is checked before TICK (31).
    This means the squire stops following before executing a follow tick when shouldStop fires.

    Do NOT register sit/stay transitions here — they are already in registerTransitions() from Plan 02-02.
    Add a registerFollowTransitions() private method call from registerTransitions() so both sit and follow
    transitions are registered in one place.

    Final registerTransitions() structure:
      private void registerTransitions() {
          registerSittingTransitions();
          registerFollowTransitions();
      }
  </action>
  <verify>
    <automated>cd C:/Users/Steve/Projects/squire-mod-v2 && ./gradlew compileJava --no-daemon 2>&1 | grep -E "error:" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "FOLLOWING_OWNER" src/main/java/com/sjviklabs/squire/brain/SquireBrain.java returns at least 2 matches (IDLE→FOLLOWING transition and FOLLOWING→IDLE transition)
    - grep "FollowHandler" src/main/java/com/sjviklabs/squire/brain/SquireBrain.java returns at least 2 matches (field decl + constructor init)
    - grep "registerFollowTransitions" src/main/java/com/sjviklabs/squire/brain/SquireBrain.java returns at least 2 matches (declaration + call)
    - grep "priority.*30\|30.*priority\|tickRate.*10\|10.*tickRate" src/main/java/com/sjviklabs/squire/brain/SquireBrain.java returns matches for follow transitions
    - ./gradlew compileJava exits 0
  </acceptance_criteria>
  <done>SquireBrain registers IDLE→FOLLOWING_OWNER, FOLLOWING_OWNER→IDLE, and per-tick FOLLOWING_OWNER action; FollowHandler initialized before registerTransitions(); full project compiles.</done>
</task>

</tasks>

<verification>
After both tasks:
- `./gradlew compileJava --no-daemon` exits 0
- grep -r "tryTeleportToOwner" src/ returns 0 matches across the entire v2 source
- grep -r "FollowHandler" src/main/java/com/sjviklabs/squire/brain/ returns matches in both handler/ and SquireBrain.java
- grep "STUCK_THRESHOLD = 60" src/main/java/com/sjviklabs/squire/brain/handler/FollowHandler.java returns a match
</verification>

<success_criteria>
- tryTeleportToOwner does not exist anywhere in the v2 source tree
- Squire follows via pathfinding only — moveTo(owner, speed) is the only navigation call
- Stuck for 60 ticks: jump boost applied, path replanned
- Sprint when distSq > sprintDistance^2 OR owner.isSprinting()
- Door navigation enabled on follow start, disabled on follow stop
- FOLLOWING_OWNER transitions registered in SquireBrain at priority 30, tickRate 10
</success_criteria>

<output>
After completion, create `.planning/phases/02-brain-fsm-follow/02-03-SUMMARY.md` following the summary template.
</output>
