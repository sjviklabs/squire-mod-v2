---
phase: 05-ui-controls
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/com/sjviklabs/squire/inventory/SquireMenu.java
  - src/main/java/com/sjviklabs/squire/client/SquireScreen.java
  - src/main/java/com/sjviklabs/squire/SquireRegistry.java
  - src/test/java/com/sjviklabs/squire/inventory/SquireMenuTest.java
autonomous: true
requirements:
  - GUI-03
must_haves:
  truths:
    - "Player can open the squire's inventory by right-clicking the squire"
    - "Equipment slots (helmet, chest, legs, boots, mainhand, offhand) appear in the GUI and accept only valid item types"
    - "Backpack grid shows correct slot count for the squire's current tier"
    - "Shift-clicking an item from player inventory places it in the correct target (equipment or backpack) without silent failure"
    - "Shift-clicking from equipment to player inventory and from backpack to player inventory both work"
    - "Squire entity preview renders in the GUI panel"
    - "Screen compiles and loads without referencing any server-only or client-only class on the wrong side"
  artifacts:
    - path: "src/main/java/com/sjviklabs/squire/inventory/SquireMenu.java"
      provides: "AbstractContainerMenu with 6 equipment + tier-backpack + player slots"
      contains: "MENU_EQUIPMENT_SLOTS"
    - path: "src/main/java/com/sjviklabs/squire/client/SquireScreen.java"
      provides: "AbstractContainerScreen rendering equipment + backpack grid + entity preview"
      exports: ["SquireScreen"]
    - path: "src/test/java/com/sjviklabs/squire/inventory/SquireMenuTest.java"
      provides: "Unit tests for slot count, equipment validation, and quickMoveStack index ranges"
      contains: "SquireMenuTest"
  key_links:
    - from: "SquireRegistry.SQUIRE_MENU"
      to: "SquireMenu constructor"
      via: "IMenuTypeExtension.create client-side factory"
      pattern: "new SquireMenu\\(windowId, inv, squire\\)"
    - from: "SquireScreen"
      to: "InventoryScreen.renderEntityInInventoryFollowsMouse"
      via: "renderBg override"
      pattern: "renderEntityInInventoryFollowsMouse"
    - from: "SquireMenu.quickMoveStack"
      to: "MENU_EQUIPMENT_SLOTS constant"
      via: "index range guards"
      pattern: "MENU_EQUIPMENT_SLOTS"
---

<objective>
Extend the Phase 1 SquireMenu stub with 6 equipment slots and build the SquireScreen client renderer on top of it.

Purpose: GUI-03 — player must be able to drag items between their inventory and the squire's equipment and backpack slots.
Output: Working inventory screen with equipment slot type validation, shift-click handling across all 4 transfer directions, entity preview, and a unit test suite covering slot layout and quickMoveStack ranges.
</objective>

<execution_context>
@C:/Users/Steve/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Steve/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-ui-controls/05-CONTEXT.md
@.planning/phases/05-ui-controls/05-RESEARCH.md
</context>

<interfaces>
<!-- Key contracts the executor needs. Read these before touching any file. -->

From src/main/java/com/sjviklabs/squire/inventory/SquireMenu.java (current stub):
- Constructor: SquireMenu(int windowId, Inventory playerInventory, SquireEntity squire)
- Current slot layout: backpackSlots (handler indices 6+), then player main inv, then hotbar
- Equipment slots (handler indices 0-5) are NOT yet in the menu — Phase 5 adds them
- IMPORTANT: adding equipment slots shifts all downstream index ranges in quickMoveStack

From src/main/java/com/sjviklabs/squire/inventory/SquireItemHandler.java:
- public static final int EQUIPMENT_SLOTS = 6;
- SLOT_HELMET=0, SLOT_CHEST=1, SLOT_LEGS=2, SLOT_BOOTS=3, SLOT_MAINHAND=4, SLOT_OFFHAND=5
- getSlots() returns EQUIPMENT_SLOTS + squire.getTier().getBackpackSlots()
- isEquipmentSlot(int slot), isBackpackSlot(int slot)

From src/main/java/com/sjviklabs/squire/entity/SquireEntity.java:
- getItemHandler(): SquireItemHandler
- getTier(): SquireTier  -- SquireTier.fromLevel(getLevel())
- getLevel(): int  -- SynchedEntityData, synced to client
- getTotalXP(): int
- getHealth(), getMaxHealth(): float
- getSquireMode(): byte  -- MODE_FOLLOW=0, MODE_SIT=1, MODE_GUARD=2
- mobInteract() already calls serverPlayer.openMenu(..., buf -> buf.writeInt(entityId))

From src/main/java/com/sjviklabs/squire/SquireRegistry.java:
- SQUIRE_MENU: DeferredHolder<MenuType<?>, MenuType<SquireMenu>>
- Currently registered with a client-side factory that reads only entityId (int, 4 bytes)
- Phase 5 DOES NOT add extra buf fields — read entity state from SynchedEntityData instead
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write test scaffold for SquireMenu slot layout and quickMoveStack</name>
  <files>src/test/java/com/sjviklabs/squire/inventory/SquireMenuTest.java</files>
  <read_first>
    - src/main/java/com/sjviklabs/squire/inventory/SquireItemHandler.java
    - src/main/java/com/sjviklabs/squire/entity/SquireEntity.java
    - src/test/java/com/sjviklabs/squire/inventory/SquireItemHandlerTest.java (existing pattern reference)
  </read_first>
  <behavior>
    - Test: Servant tier menu has 6 equipment + 9 backpack + 27 player main + 9 hotbar = 51 total slots
    - Test: Champion tier menu has 6 equipment + 36 backpack + 27 player main + 9 hotbar = 78 total slots
    - Test: MENU_EQUIPMENT_SLOTS constant equals 6
    - Test: Equipment slot at index 0 does NOT accept a pickaxe (mayPlace returns false for non-armor)
    - Test: Equipment slot at index 0 DOES accept a helmet (mayPlace returns true for head armor)
    - Test: Mainhand slot (index 4) accepts any item (mayPlace always true)
    - Test: quickMoveStack with index 0 (equipment slot) moves item toward player inventory range
    - Test: quickMoveStack with index from player inventory range moves item first toward equipment (if valid) then backpack
    - Use TestableItemHandler test double pattern from existing tests (squire.getLevel() mocked to return 0 = Servant)
  </behavior>
  <action>
    Create src/test/java/com/sjviklabs/squire/inventory/SquireMenuTest.java.

    Use the TestableItemHandler pattern already established in Phase 1 tests. Tests must be headless (JUnit 5, no NeoForge bootstrap). SquireMenu takes a SquireEntity — use a mock or a test double that returns a TestableItemHandler and a hardcoded tier.

    All tests must FAIL before Task 2 implements the new SquireMenu. That is the RED step. Do not write passing tests against the current stub — the stub does not have equipment slots yet.

    Run after writing: ./gradlew test --tests "*.SquireMenuTest"
    Expected: tests compile but FAIL (RED) because SquireMenu doesn't yet have MENU_EQUIPMENT_SLOTS or equipment slot logic.
  </action>
  <verify>
    <automated>cd C:/Users/Steve/Projects/squire-mod-v2 && ./gradlew test --tests "*.SquireMenuTest" 2>&1 | tail -20</automated>
  </verify>
  <done>Test file exists, compiles, and at least one test fails (RED state confirmed). No test should pass yet because SquireMenu is still the Phase 1 stub.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend SquireMenu with equipment slots and build SquireScreen</name>
  <files>
    src/main/java/com/sjviklabs/squire/inventory/SquireMenu.java
    src/main/java/com/sjviklabs/squire/client/SquireScreen.java
  </files>
  <read_first>
    - src/main/java/com/sjviklabs/squire/inventory/SquireMenu.java
    - src/main/java/com/sjviklabs/squire/inventory/SquireItemHandler.java
    - src/main/java/com/sjviklabs/squire/SquireRegistry.java
    - src/main/java/com/sjviklabs/squire/entity/SquireEntity.java
    - C:/Users/Steve/Projects/squire-mod/src/main/java/com/sjviklabs/squire/inventory/SquireScreen.java (v0.5.0 reference)
    - .planning/phases/05-ui-controls/05-RESEARCH.md
  </read_first>
  <behavior>
    All SquireMenuTest tests from Task 1 must pass (GREEN) after this task.
  </behavior>
  <action>
    PART A — Extend SquireMenu.java:

    1. Add constant: `public static final int MENU_EQUIPMENT_SLOTS = 6;`

    2. Change constructor to register equipment slots FIRST (before backpack), using an inner static class EquipmentSlotTyped that extends SlotItemHandler and overrides mayPlace:
       - Slot 0 (SLOT_HELMET): only head armor — stack.canEquip(EquipmentSlot.HEAD, null)
       - Slot 1 (SLOT_CHEST): only chest armor — stack.canEquip(EquipmentSlot.CHEST, null)
       - Slot 2 (SLOT_LEGS): only leg armor — stack.canEquip(EquipmentSlot.LEGS, null)
       - Slot 3 (SLOT_BOOTS): only feet armor — stack.canEquip(EquipmentSlot.FEET, null)
       - Slot 4 (SLOT_MAINHAND): any item — always return true
       - Slot 5 (SLOT_OFFHAND): any item — always return true
       Screen coordinates: column of 6 slots on the left panel, e.g. x=8, y starting at 8, 18px spacing.

    3. After equipment slots, register backpack slots (handler indices EQUIPMENT_SLOTS through EQUIPMENT_SLOTS+backpackSlots-1) in a 9-column grid. Backpack grid x=62, y=8 (adjust to match v0.5.0 layout).

    4. After backpack, register player main inventory and hotbar as before. Update all index references to use MENU_EQUIPMENT_SLOTS.

    5. Rewrite quickMoveStack to handle 4 directions:
       - index < MENU_EQUIPMENT_SLOTS (equipment slot): move to player inv (backpackEnd..slots.size())
       - index in [MENU_EQUIPMENT_SLOTS, backpackEnd): move to player inv
       - index >= backpackEnd (player inv or hotbar): try equipment first (if valid via mayPlace check), then try backpack
       Use `AbstractContainerMenu.moveItemStackTo(stack, fromInclusive, toExclusive, reverseOrder)` — do NOT write a manual loop.
       Constants to define: int backpackEnd = MENU_EQUIPMENT_SLOTS + backpackSlots; int playerEnd = backpackEnd + 36;

    PART B — Create SquireScreen.java in src/main/java/com/sjviklabs/squire/client/:

    File header: package com.sjviklabs.squire.client; — CLIENT ONLY. No server imports permitted.

    Class: public class SquireScreen extends AbstractContainerScreen<SquireMenu>

    Key methods:
    - Constructor takes (SquireMenu menu, Inventory inv, Component title)
    - init(): set imageWidth=230 (wide for equipment + backpack side-by-side), imageHeight=200
    - renderBg(): draw background texture (use a solid color fill or simple rect if no texture yet — do not crash on missing texture). Call InventoryScreen.renderEntityInInventoryFollowsMouse() for the entity preview panel at x=45, y=60, scale=30.
    - renderLabels(): draw "Equipment" and "Inventory" section labels

    Texture: if no squire_screen.png exists yet, use GuiGraphics.fill() for panel backgrounds (dark gray = 0xFF202020, light gray = 0xFF303030). This avoids a missing-texture crash while visual polish is deferred.

    Registration: SquireScreen must be registered against SquireRegistry.SQUIRE_MENU in a client-side event. Register in SquireClientEvents (created in Plan 05-02). Add a static registerScreens method on SquireScreen to be called from there:
    ```java
    public static void register() {
        MenuScreens.register(SquireRegistry.SQUIRE_MENU.get(), SquireScreen::new);
    }
    ```
    This method will be called from SquireClientEvents.onRegisterScreens (Plan 05-02 wires this). In this plan, create SquireScreen.register() but do NOT yet call it — Plan 05-02 creates SquireClientEvents.

    Run after implementing: ./gradlew test --tests "*.SquireMenuTest"
    All SquireMenuTest tests must pass (GREEN).
  </action>
  <verify>
    <automated>cd C:/Users/Steve/Projects/squire-mod-v2 && ./gradlew test --tests "*.SquireMenuTest" 2>&1 | tail -20</automated>
  </verify>
  <done>
    - SquireMenuTest: all tests GREEN
    - SquireMenu.MENU_EQUIPMENT_SLOTS = 6 exists
    - Equipment slot 0 mayPlace rejects a pickaxe and accepts a helmet
    - quickMoveStack handles all 4 directions without silent failure
    - SquireScreen.java exists in client/ package with no server-side imports
    - ./gradlew build compiles without error
  </done>
</task>

</tasks>

<verification>
Run full test suite: cd C:/Users/Steve/Projects/squire-mod-v2 && ./gradlew test

Grep checks:
- grep -r "MENU_EQUIPMENT_SLOTS" src/main/java/com/sjviklabs/squire/inventory/SquireMenu.java — must find the constant
- grep -r "mayPlace" src/main/java/com/sjviklabs/squire/inventory/SquireMenu.java — must find equipment slot validation
- grep -r "import net.minecraft.client\|import net.minecraft.server" src/main/java/com/sjviklabs/squire/client/SquireScreen.java — must NOT find server imports in client class
- grep -r "renderEntityInInventoryFollowsMouse" src/main/java/com/sjviklabs/squire/client/SquireScreen.java — must find entity preview call
</verification>

<success_criteria>
- SquireMenu has exactly MENU_EQUIPMENT_SLOTS (6) equipment slots registered before backpack slots
- Equipment slots validate item type via mayPlace() override
- quickMoveStack handles all 4 transfer directions using moveItemStackTo, not manual loops
- SquireScreen lives in client/ package, registers via SquireScreen.register(), has entity preview
- All SquireMenuTest tests pass
- ./gradlew build succeeds
</success_criteria>

<output>
After completion, create .planning/phases/05-ui-controls/05-01-SUMMARY.md
</output>
