---
phase: 05-ui-controls
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/com/sjviklabs/squire/client/SquireKeybinds.java
  - src/main/java/com/sjviklabs/squire/client/SquireClientEvents.java
  - src/main/java/com/sjviklabs/squire/client/SquireRadialScreen.java
  - src/test/java/com/sjviklabs/squire/client/SquireRadialAngleTest.java
autonomous: true
requirements:
  - GUI-01
must_haves:
  truths:
    - "Pressing R while looking at the squire within interaction range opens the radial menu"
    - "Radial menu shows exactly 4 wedges: Follow, Stay, Guard, Inventory"
    - "Hovering a wedge highlights it; releasing R (or clicking) fires the corresponding CMD_ payload to the server"
    - "Radial menu does NOT pause the game — server keeps ticking in multiplayer"
    - "Pressing Escape or R again closes the radial without sending any command"
    - "R keybind appears in the Controls screen under 'Squire' category without conflicting with default keybinds"
    - "SquireClientEvents uses Dist.CLIENT so it is never loaded on a dedicated server"
  artifacts:
    - path: "src/main/java/com/sjviklabs/squire/client/SquireKeybinds.java"
      provides: "KeyMapping constant for radial menu R key"
      contains: "RADIAL_MENU"
    - path: "src/main/java/com/sjviklabs/squire/client/SquireClientEvents.java"
      provides: "Mod-bus + game-bus subscriber, Dist.CLIENT only, wires keybind, screen registration, radial open"
      exports: ["SquireClientEvents"]
    - path: "src/main/java/com/sjviklabs/squire/client/SquireRadialScreen.java"
      provides: "4-wedge radial overlay, isPauseScreen=false, fires SquireCommandPayload"
      exports: ["SquireRadialScreen"]
    - path: "src/test/java/com/sjviklabs/squire/client/SquireRadialAngleTest.java"
      provides: "Unit tests for wedge angle calculation (pure math, no client dep)"
      contains: "SquireRadialAngleTest"
  key_links:
    - from: "SquireClientEvents.onKeyInput"
      to: "SquireRadialScreen"
      via: "Minecraft.getInstance().setScreen(new SquireRadialScreen(squire))"
      pattern: "setScreen.*SquireRadialScreen"
    - from: "SquireRadialScreen.onClose / mouseClicked"
      to: "SquireCommandPayload"
      via: "PacketDistributor.sendToServer"
      pattern: "sendToServer.*SquireCommandPayload"
    - from: "SquireClientEvents"
      to: "SquireScreen.register()"
      via: "RegisterMenuScreensEvent or FMLClientSetupEvent"
      pattern: "SquireScreen.register"
---

<objective>
Create the radial menu overlay (R key) and all client-side event wiring for Phase 5.

Purpose: GUI-01 — player presses R while looking at the squire to select Follow, Stay, Guard, or Inventory via a 4-wedge radial overlay. Commands fire as StreamCodec payloads to the server.
Output: SquireKeybinds, SquireClientEvents, SquireRadialScreen, and the angle-calculation unit test. The Dist.CLIENT boundary is enforced — none of these classes are ever loaded server-side.
</objective>

<execution_context>
@C:/Users/Steve/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Steve/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-ui-controls/05-CONTEXT.md
@.planning/phases/05-ui-controls/05-RESEARCH.md
</context>

<interfaces>
<!-- Key contracts the executor needs. -->

From .planning/phases/05-ui-controls/05-RESEARCH.md (Pattern 4 — Radial Menu):
- SquireRadialScreen extends Screen, NOT AbstractContainerScreen
- isPauseScreen() MUST return false — returning true stops server tick in multiplayer
- Wedge angle convention: angle 0 = top (−Y), increases clockwise
  float angle = (float) Math.atan2(dx, -dy);
  if (angle < 0) angle += (float)(2.0 * Math.PI);
  hoveredWedge = (int)(angle / WEDGE_ANGLE);  // WEDGE_ANGLE = 2π/4 for 4 wedges
- Keybind registration: @EventBusSubscriber(modid=..., bus=Bus.MOD, value=Dist.CLIENT)
- Key input handling: @EventBusSubscriber(modid=..., bus=Bus.GAME, value=Dist.CLIENT)
  NOTE: RegisterKeyMappingsEvent is on MOD bus; InputEvent.Key is on GAME bus
  Cleanest approach: one class with two separate @EventBusSubscriber annotations on inner classes, or split into SquireKeybinds (constants) + SquireClientEvents (all events on both buses)

From src/main/java/com/sjviklabs/squire/entity/SquireEntity.java:
- SquireEntity is the entity type to raycast for
- MODE_FOLLOW=0, MODE_SIT=1, MODE_GUARD=2 (byte constants)

SquireCommandPayload (exists from Phase 2, plan 02-04 — or create stub in this plan):
- CMD_FOLLOW=0, CMD_STAY=2, CMD_GUARD=1, CMD_INVENTORY=3
- record SquireCommandPayload(int commandId, int squireEntityId)
- PacketDistributor.sendToServer(new SquireCommandPayload(cmd, squire.getId()))
- If SquireCommandPayload does not yet exist (Phase 2 not complete), create the class in this plan at src/main/java/com/sjviklabs/squire/network/SquireCommandPayload.java with the 4 constants and full StreamCodec. Plan 05-03 will extend it.

SquireScreen.register() (created in Plan 05-01):
- Call from SquireClientEvents during FMLClientSetupEvent or RegisterMenuScreensEvent
- MenuScreens.register(SquireRegistry.SQUIRE_MENU.get(), SquireScreen::new)
- If Plan 05-01 has not yet run, add a TODO comment here and register it once 05-01 is complete
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write angle-calculation unit test (RED)</name>
  <files>src/test/java/com/sjviklabs/squire/client/SquireRadialAngleTest.java</files>
  <read_first>
    - .planning/phases/05-ui-controls/05-RESEARCH.md (Pattern 4, Radial Wedge Angle Convention)
  </read_first>
  <behavior>
    - Test: cursor directly above center (dx=0, dy=-1) → wedge 0 (Follow)
    - Test: cursor directly right of center (dx=1, dy=0) → wedge 1 (Stay)
    - Test: cursor directly below center (dx=0, dy=1) → wedge 2 (Guard)
    - Test: cursor directly left of center (dx=-1, dy=0) → wedge 3 (Inventory)
    - Test: negative angle from atan2 is normalized to positive before index calculation
    - Test: angle exactly at boundary (π/2) maps to wedge 1 (right boundary)
    These tests call a static utility method SquireRadialScreen.computeWedge(float dx, float dy) which doesn't exist yet — tests will fail to compile until Task 2 creates it.
    Use @Disabled on the class if it causes compile failure before SquireRadialScreen exists — then remove @Disabled in Task 2.
  </behavior>
  <action>
    Create src/test/java/com/sjviklabs/squire/client/SquireRadialAngleTest.java.

    Tests call: SquireRadialScreen.computeWedge(float dx, float dy) — a package-level static method.
    The computation:
      float angle = (float) Math.atan2(dx, -dy);
      if (angle < 0) angle += (float)(2.0 * Math.PI);
      return (int)(angle / WEDGE_ANGLE);  // WEDGE_ANGLE = (float)(Math.PI / 2)

    Use @Disabled if needed to allow the build to proceed before SquireRadialScreen exists. Remove @Disabled in Task 2.

    Run: ./gradlew test --tests "*.SquireRadialAngleTest"
    Expected: compile error or skipped (RED — class doesn't exist yet).
  </action>
  <verify>
    <automated>cd C:/Users/Steve/Projects/squire-mod-v2 && ./gradlew test --tests "*.SquireRadialAngleTest" 2>&1 | tail -20</automated>
  </verify>
  <done>Test file exists. If SquireRadialScreen doesn't exist yet, test is @Disabled or fails to compile — that is the expected RED state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create SquireKeybinds, SquireRadialScreen, and SquireClientEvents</name>
  <files>
    src/main/java/com/sjviklabs/squire/client/SquireKeybinds.java
    src/main/java/com/sjviklabs/squire/client/SquireRadialScreen.java
    src/main/java/com/sjviklabs/squire/client/SquireClientEvents.java
  </files>
  <read_first>
    - .planning/phases/05-ui-controls/05-RESEARCH.md (Patterns 3, 4; Pitfall 1; Code Examples section)
    - C:/Users/Steve/Projects/squire-mod/src/main/java/com/sjviklabs/squire/client/SquireRadialScreen.java (v0.5.0 reference)
  </read_first>
  <behavior>
    - SquireRadialAngleTest: all 6 tests GREEN after SquireRadialScreen.computeWedge exists
  </behavior>
  <action>
    FILE 1 — SquireKeybinds.java:
    ```
    package com.sjviklabs.squire.client;

    import com.mojang.blaze3d.platform.InputConstants;
    import net.minecraft.client.KeyMapping;
    import net.neoforged.neoforge.client.settings.KeyConflictContext;
    import org.lwjgl.glfw.GLFW;

    public final class SquireKeybinds {
        public static final KeyMapping RADIAL_MENU = new KeyMapping(
            "key.squire.radial_menu",
            KeyConflictContext.IN_GAME,
            InputConstants.Type.KEYSYM,
            GLFW.GLFW_KEY_R,
            "key.categories.squire"
        );
        private SquireKeybinds() {}
    }
    ```

    FILE 2 — SquireRadialScreen.java:
    - package com.sjviklabs.squire.client; — CLIENT ONLY
    - extends Screen
    - isPauseScreen(): return false  (CRITICAL — never true)
    - Constructor: takes SquireEntity squire, stores as field
    - 4 wedges: WEDGE_LABELS = {"Follow", "Stay", "Guard", "Inventory"}
    - WEDGE_CMDS = {CMD_FOLLOW, CMD_STAY, CMD_GUARD, CMD_INVENTORY}  (use SquireCommandPayload constants)
    - WEDGE_ANGLE = (float)(Math.PI / 2)  (π/2 per wedge for 4 wedges)
    - RING_INNER = 30, RING_OUTER = 80 (pixels from center)
    - static computeWedge(float dx, float dy): uses the atan2 convention from RESEARCH.md
      float angle = (float)Math.atan2(dx, -dy);
      if (angle < 0) angle += (float)(2.0 * Math.PI);
      return (int)(angle / WEDGE_ANGLE);
    - render(): for each of 4 wedges draw a filled TRIANGLE_FAN from inner ring to outer ring using RenderSystem.setShader(GameRenderer::getPositionColorShader). Hovered wedge is a brighter color. Draw wedge label text at midpoint of each wedge arc. Use GuiGraphics for text.
    - mouseMoved(double mx, double my): compute dx = mx - width/2, dy = my - height/2, update hoveredWedge. Only update if cursor is between RING_INNER and RING_OUTER distance from center.
    - mouseClicked(): if hoveredWedge >= 0 and >= 0 and < 4, fire command and close
    - keyPressed(): if key == radial keybind key or key == GLFW_KEY_ESCAPE → close without firing
    - onClose(): if hoveredWedge >= 0 and valid, PacketDistributor.sendToServer(new SquireCommandPayload(WEDGE_CMDS[hoveredWedge], squire.getId()))
    NOTE: fire the packet in mouseClicked for explicit click, and in onClose only if triggered by a non-cancel action. Use a boolean `confirmed` field: set true in mouseClicked, check in onClose.
    - renderBackground(): no-op (don't dim the background — radial stays transparent over the world)

    FILE 3 — SquireClientEvents.java:
    - package com.sjviklabs.squire.client; — CLIENT ONLY
    - Use TWO @EventBusSubscriber annotations on the class or split into inner classes:
      Mod bus handlers: RegisterKeyMappingsEvent → event.register(SquireKeybinds.RADIAL_MENU)
                        FMLClientSetupEvent → SquireScreen.register() (if Plan 05-01 complete)
      Game bus handlers: InputEvent.Key → open radial if RADIAL_MENU.consumeClick() && mc.screen == null
    - Radial open check: raycast via mc.player.pick(range, 0, false), check if result is EntityHitResult with SquireEntity
    - Range: use mc.player.getBlockReach() or default 4.5f if null
    - @EventBusSubscriber(modid = SquireMod.MODID, bus = Bus.MOD, value = Dist.CLIENT) for mod-bus methods
    - @EventBusSubscriber(modid = SquireMod.MODID, bus = Bus.GAME, value = Dist.CLIENT) for game-bus methods
    - Cleanest pattern: one class, two separate annotated inner static classes (ModEvents and GameEvents), or annotate the outer class twice (NeoForge supports multiple @EventBusSubscriber on one class via repeated annotation).

    Remove @Disabled from SquireRadialAngleTest (if it was added in Task 1).

    Run: ./gradlew test --tests "*.SquireRadialAngleTest"
    Expected: all 6 tests GREEN.
  </action>
  <verify>
    <automated>cd C:/Users/Steve/Projects/squire-mod-v2 && ./gradlew test --tests "*.SquireRadialAngleTest" 2>&1 | tail -20</automated>
  </verify>
  <done>
    - SquireRadialAngleTest: all tests GREEN
    - SquireKeybinds.RADIAL_MENU constant exists with R key default and "key.categories.squire" category
    - SquireRadialScreen.isPauseScreen() returns false
    - SquireRadialScreen.computeWedge(float,float) is static and package-accessible
    - SquireClientEvents has Dist.CLIENT on both subscriptions — safe on dedicated server
    - ./gradlew build compiles without error
    - grep -r "Dist.CLIENT" src/main/java/com/sjviklabs/squire/client/SquireClientEvents.java returns matches
  </done>
</task>

</tasks>

<verification>
Run full test suite: cd C:/Users/Steve/Projects/squire-mod-v2 && ./gradlew test

Grep checks:
- grep "isPauseScreen" src/main/java/com/sjviklabs/squire/client/SquireRadialScreen.java — must find "return false"
- grep "Dist.CLIENT" src/main/java/com/sjviklabs/squire/client/SquireClientEvents.java — must appear on all @EventBusSubscriber annotations
- grep "RADIAL_MENU" src/main/java/com/sjviklabs/squire/client/SquireKeybinds.java — must find KeyMapping constant
- grep -r "import net.minecraft.server" src/main/java/com/sjviklabs/squire/client/ — must return nothing
</verification>

<success_criteria>
- SquireRadialAngleTest all tests pass
- 4-wedge radial screen exists with correct angle math
- isPauseScreen() returns false
- All 3 client classes are in client/ package with no server imports
- SquireClientEvents uses Dist.CLIENT for all event subscriptions
- ./gradlew build succeeds
</success_criteria>

<output>
After completion, create .planning/phases/05-ui-controls/05-02-SUMMARY.md
</output>
