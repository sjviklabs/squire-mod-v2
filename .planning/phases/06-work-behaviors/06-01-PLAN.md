---
phase: 06-work-behaviors
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/com/sjviklabs/squire/brain/handler/TorchHandler.java
  - src/main/java/com/sjviklabs/squire/brain/handler/MiningHandler.java
  - src/test/java/com/sjviklabs/squire/brain/handler/TorchHandlerTest.java
  - src/test/java/com/sjviklabs/squire/brain/handler/MiningHandlerTest.java
  - src/test/java/com/sjviklabs/squire/gametest/MiningHandlerGameTest.java
autonomous: true
requirements: [WRK-01, WRK-02, WRK-03]

must_haves:
  truths:
    - "Squire auto-places a torch when ambient light at its position drops below the configured threshold (default 7) while following"
    - "Squire breaks a single targeted block and deposits the drops into its IItemHandler inventory"
    - "Squire queues and breaks all blocks in a specified area (top-down, nearest-first), loading chunks via ARC-09 before scanning"
    - "Mining progress uses vanilla destroyBlockProgress overlay; break speed scales with tool and tier"
  artifacts:
    - path: "src/main/java/com/sjviklabs/squire/brain/handler/TorchHandler.java"
      provides: "Side-effect torch placer called from FollowHandler tick"
      contains: "class TorchHandler"
    - path: "src/main/java/com/sjviklabs/squire/brain/handler/MiningHandler.java"
      provides: "FSM MINING_APPROACH → MINING_BREAK cycle, area clear queue"
      contains: "class MiningHandler"
    - path: "src/test/java/com/sjviklabs/squire/brain/handler/TorchHandlerTest.java"
      provides: "Unit tests: light threshold gate, torch slot search, cooldown"
      contains: "class TorchHandlerTest"
    - path: "src/test/java/com/sjviklabs/squire/brain/handler/MiningHandlerTest.java"
      provides: "Unit tests: area queue ordering (top-down), stuck skip after 3 attempts"
      contains: "class MiningHandlerTest"
    - path: "src/test/java/com/sjviklabs/squire/gametest/MiningHandlerGameTest.java"
      provides: "GameTest: single block mine deposits drops into squire inventory"
      contains: "class MiningHandlerGameTest"
  key_links:
    - from: "FollowHandler.tick()"
      to: "TorchHandler.tryPlaceTorch()"
      via: "direct method call, not an event"
      pattern: "torchHandler\\.tryPlaceTorch\\(\\)"
    - from: "MiningHandler.setTarget(BlockPos)"
      to: "SquireBrain.forceState(MINING_APPROACH)"
      via: "machine.forceState()"
      pattern: "machine\\.forceState\\(SquireAIState\\.MINING_APPROACH\\)"
    - from: "MiningHandler.tickBreak() deposits"
      to: "squire.getItemHandler().insertItem()"
      via: "IItemHandler capability"
      pattern: "getItemHandler\\(\\)\\.insertItem"
    - from: "MiningHandler.setAreaTarget()"
      to: "squire.ensureChunkLoaded()"
      via: "call before building block queue"
      pattern: "ensureChunkLoaded"
---

<objective>
Implement TorchHandler (side-effect torch placement) and MiningHandler (single-block and area-clear mining) with test scaffolds first. TorchHandler validates the Phase 6 handler pattern at lowest complexity; MiningHandler is the most-used work command.

Purpose: Squire can light dark areas while following and mine on command — the foundational work behaviors. All later handlers follow the same approach/work cycle established here.
Output: TorchHandler, MiningHandler, 3 test files (2 unit + 1 GameTest stub).
</objective>

<execution_context>
@C:/Users/Steve/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Steve/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/Steve/Projects/squire-mod-v2/.planning/PROJECT.md
@C:/Users/Steve/Projects/squire-mod-v2/.planning/ROADMAP.md
@C:/Users/Steve/Projects/squire-mod-v2/.planning/phases/06-work-behaviors/06-CONTEXT.md
@C:/Users/Steve/Projects/squire-mod-v2/.planning/phases/06-work-behaviors/06-RESEARCH.md

<!-- v0.5.0 reference (read-only — adapt to v2 IItemHandler API) -->
@C:/Users/Steve/Projects/squire-mod/src/main/java/com/sjviklabs/squire/ai/handler/TorchHandler.java
@C:/Users/Steve/Projects/squire-mod/src/main/java/com/sjviklabs/squire/ai/handler/MiningHandler.java

<interfaces>
<!-- Key contracts the executor needs. No codebase exploration required. -->

From src/main/java/com/sjviklabs/squire/brain/SquireAIState.java (Phase 2):
  IDLE, FOLLOWING_OWNER,
  MINING_APPROACH, MINING_BREAK

From src/main/java/com/sjviklabs/squire/entity/SquireEntity.java (Phase 1):
  public SquireItemHandler getItemHandler()         // IItemHandler for inventory access
  public SquireTier getTier()                       // current tier (SERVANT..CHAMPION)
  public int getSquireLevel()                       // numeric level for speed bonuses
  public void ensureChunkLoaded(BlockPos pos)       // stub — calls ForceChunkManager in Phase 6 impl

From src/main/java/com/sjviklabs/squire/inventory/SquireItemHandler.java (Phase 1):
  public ItemStack getStackInSlot(int slot)
  public ItemStack extractItem(int slot, int amount, boolean simulate)
  public ItemStack insertItem(int slot, ItemStack stack, boolean simulate)
  public int getSlots()                             // tier-gated: 6 equipment + backpack slots

From src/main/java/com/sjviklabs/squire/brain/TickRateStateMachine.java (Phase 2):
  public void forceState(SquireAIState state)
  public SquireAIState getState()

From src/main/java/com/sjviklabs/squire/brain/SquireBrainEventBus.java (Phase 2):
  public void fire(SquireEvent event, SquireEntity squire)

From src/main/java/com/sjviklabs/squire/config/SquireConfig.java (Phase 1):
  // Mining section — all these exist:
  public static ForgeConfigSpec.IntValue miningReach;          // default 4
  public static ForgeConfigSpec.DoubleValue breakSpeedMultiplier; // default 1.0
  public static ForgeConfigSpec.DoubleValue miningSpeedPerLevel;  // bonus per level
  public static ForgeConfigSpec.IntValue areaMaxBlocks;         // default 256
  // Torch section:
  public static ForgeConfigSpec.IntValue torchLightThreshold;   // default 7
  public static ForgeConfigSpec.IntValue torchCheckInterval;    // default 20 ticks
  public static ForgeConfigSpec.IntValue torchPlaceCooldown;    // default 40 ticks
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test scaffolds — TorchHandlerTest, MiningHandlerTest, MiningHandlerGameTest</name>
  <files>
    src/test/java/com/sjviklabs/squire/brain/handler/TorchHandlerTest.java
    src/test/java/com/sjviklabs/squire/brain/handler/MiningHandlerTest.java
    src/test/java/com/sjviklabs/squire/gametest/MiningHandlerGameTest.java
  </files>
  <read_first>
    - src/test/java/com/sjviklabs/squire/inventory/SquireItemHandlerTest.java (TestableItemHandler double pattern to reuse)
    - src/test/java/com/sjviklabs/squire/gametest/SquireEntityGameTest.java (@GameTestHolder annotation pattern)
    - C:/Users/Steve/Projects/squire-mod-v2/.planning/phases/06-work-behaviors/06-RESEARCH.md (validation architecture section, WRK-01..03 test map)
  </read_first>
  <behavior>
    TorchHandlerTest (unit, no server):
    - test_noTorch_doesNotPlace: squire inventory empty → tryPlaceTorch() returns false, no block placed
    - test_aboveThreshold_doesNotPlace: light level 8 (above default 7) → tryPlaceTorch() returns false
    - test_belowThreshold_withTorch_places: light level 6, torch in slot 7 → tryPlaceTorch() returns true, torch slot count decremented
    - test_cooldown_blocksPlacement: place once, call again before cooldown expires → returns false
    - test_torchSlotSearch_findsFirstMatch: torch in slot 10 (not slot 0) → correctly found

    MiningHandlerTest (unit, no server):
    - test_areaQueue_topDown: area Y=60 to Y=62 → blocks enqueued top-down (Y=62 first)
    - test_areaQueue_nearestFirst: blocks at varying XZ distance → nearer block appears before farther at same Y
    - test_stuckSkip_afterMaxAttempts: stuck counter reaches MAX_REPOSITION_ATTEMPTS (3) → target popped, logged, moves to next
    - test_setTarget_null_doesNothing: setTarget(null) → handler stays IDLE

    MiningHandlerGameTest (GameTest stub — compiles, calls helper.succeed() immediately):
    - test_singleBlockMine_depositsDrops: TODO Phase 6 execution — stub for now
  </behavior>
  <action>
    Create the three test files. Tests must be FAILING (RED) before Task 2 implements the handlers.

    TorchHandlerTest: Use a TestableTorchHandler inner class (same double pattern as TestableItemHandler) that accepts SquireTier + light level directly rather than requiring live SquireEntity. Mock the IItemHandler with a simple slot array. Test torch slot search by walking slots. Cooldown test: call tryPlaceTorch() twice with zero elapsed ticks between calls — second call must return false.

    MiningHandlerTest: Use a TestableMiningHandler inner class. setAreaTarget(cornerA, cornerB) must populate a queue. Test top-down ordering: supply cornerA=(0,60,0) cornerB=(2,62,2) → poll() chain must yield Y=62 blocks before Y=61 before Y=60. For nearest-first: two blocks same Y but different XZ distance from squire position — closer block polled first. Stuck skip: call incrementStuck() in a loop to MAX_REPOSITION_ATTEMPTS (constant 3), then assert popNearestReachable() skips that entry.

    MiningHandlerGameTest: Annotate with @GameTestHolder(value = "squire") and @PrefixGameTestTemplate(false). Template is "squire:empty" (already registered Phase 1). Single test method test_singleBlockMine_depositsDrops — body is helper.succeed() with a TODO comment describing what Phase 6 execution must wire.

    Run ./gradlew test — tests MUST fail with method-not-found or class-not-found errors (RED state confirmed). If tests pass before Task 2, the doubles are wrong — fix them.
  </action>
  <verify>
    <automated>cd C:/Users/Steve/Projects/squire-mod-v2 && ./gradlew compileTestJava 2>&1 | tail -20</automated>
  </verify>
  <done>All three test files compile. TorchHandlerTest and MiningHandlerTest fail at runtime (RED) because TorchHandler and MiningHandler classes do not exist yet. MiningHandlerGameTest compiles and the stub is registered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TorchHandler — side-effect torch placement while following</name>
  <files>
    src/main/java/com/sjviklabs/squire/brain/handler/TorchHandler.java
  </files>
  <read_first>
    - C:/Users/Steve/Projects/squire-mod/src/main/java/com/sjviklabs/squire/ai/handler/TorchHandler.java (v0.5.0 reference)
    - src/main/java/com/sjviklabs/squire/config/SquireConfig.java (torchLightThreshold, torchCheckInterval, torchPlaceCooldown)
    - src/main/java/com/sjviklabs/squire/inventory/SquireItemHandler.java (getStackInSlot, extractItem)
  </read_first>
  <behavior>
    - tryPlaceTorch() returns false when squire inventory has no torch (Items.TORCH scan of all slots)
    - tryPlaceTorch() returns false when light level at squire position >= SquireConfig.torchLightThreshold.get()
    - tryPlaceTorch() returns false when cooldown has not expired (ticksSinceLastPlace < SquireConfig.torchPlaceCooldown.get())
    - tryPlaceTorch() places torch at squire's feet on a solid block, calls extractItem(slot, 1, false), returns true
    - Placement logic: find floor block (squire blockPos.below()), confirm it is solid and the position above is air, then level.setBlock(floorPos.above(), Blocks.TORCH.defaultBlockState(), 3)
    - tick() increments ticksSinceLastPlace and calls tryPlaceTorch() only every SquireConfig.torchCheckInterval.get() ticks
  </behavior>
  <action>
    Port v0.5.0 TorchHandler with these v2 changes:
    - Replace squire.getSquireInventory().getItem(i) with squire.getItemHandler().getStackInSlot(i)
    - Replace removeItem(slot, 1) with squire.getItemHandler().extractItem(slot, 1, false)
    - Replace SquireAbilities.hasAutoTorch() with squire.getSquireLevel() >= SquireConfig.torchAbilityMinLevel.get() (use config int; if that key doesn't exist in SquireConfig, use tier ordinal >= 1 as fallback and add a TODO)
    - TorchHandler is NOT a FSM state — it has no APPROACH/WORK cycle. Constructor: TorchHandler(SquireEntity squire). Public API: void tick(), boolean tryPlaceTorch().
    - Use SquireConfig.activityLogging.get() guard on any debug log lines.
    - Do NOT add TorchHandler to SquireBrain registration in this task — FollowHandler calls it directly (Phase 3 plan will wire it). Add a code comment: "Wire: FollowHandler calls torchHandler.tryPlaceTorch() at end of its tick method (Phase 3 wiring plan)."

    After writing TorchHandler, run ./gradlew test — TorchHandlerTest MUST go GREEN.
  </action>
  <verify>
    <automated>cd C:/Users/Steve/Projects/squire-mod-v2 && ./gradlew test --tests "com.sjviklabs.squire.brain.handler.TorchHandlerTest" 2>&1 | tail -20</automated>
  </verify>
  <done>TorchHandlerTest all assertions pass (GREEN). ./gradlew compileJava exits 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: MiningHandler — MINING_APPROACH → MINING_BREAK cycle with area clear</name>
  <files>
    src/main/java/com/sjviklabs/squire/brain/handler/MiningHandler.java
  </files>
  <read_first>
    - C:/Users/Steve/Projects/squire-mod/src/main/java/com/sjviklabs/squire/ai/handler/MiningHandler.java (v0.5.0 reference)
    - src/main/java/com/sjviklabs/squire/config/SquireConfig.java (miningReach, breakSpeedMultiplier, miningSpeedPerLevel, areaMaxBlocks)
    - src/main/java/com/sjviklabs/squire/brain/TickRateStateMachine.java (forceState)
    - C:/Users/Steve/Projects/squire-mod-v2/.planning/phases/06-work-behaviors/06-RESEARCH.md (break speed formula, area clear pitfall 4)
  </read_first>
  <behavior>
    Single-block flow:
    - setTarget(BlockPos pos): stores target, calls machine.forceState(MINING_APPROACH)
    - MINING_APPROACH tick: squire pathfinds to within SquireConfig.miningReach.get() blocks of target; stuck detection (approachTicks, lastApproachDistSq, STUCK_TIMEOUT=100); on arrival → machine.forceState(MINING_BREAK)
    - MINING_BREAK tick: call level.destroyBlockProgress(squire.getId(), target, stage) each tick; accumulate breakProgress using formula from RESEARCH.md; on breakProgress >= 1.0 → level.destroyBlock(target, true), collect drops, insertItem into squire inventory via getItemHandler().insertItem(); overflow drops at feet via spawnAtLocation(); machine.forceState(IDLE); fire SquireEvent.WORK_TASK_COMPLETE

    Area clear flow:
    - setAreaTarget(BlockPos cornerA, BlockPos cornerB): call squire.ensureChunkLoaded(pos) for each corner, build LinkedList<BlockPos> queue sorted top-down (Y desc) then nearest-first (distance to squire asc within same Y); cap at SquireConfig.areaMaxBlocks.get(); calls machine.forceState(MINING_APPROACH)
    - On each MINING_BREAK completion: poll next block from queue; if queue empty → machine.forceState(IDLE) and fire WORK_TASK_COMPLETE

    Stuck detection: if approachTicks > STUCK_TIMEOUT and distance improvement < 0.1 blocks, increment repositionAttempts; if repositionAttempts >= MAX_REPOSITION_ATTEMPTS (3) → pop target/queue entry, log "squire stuck, skipping block", return to IDLE or next queue entry

    Break speed formula (port unchanged from v0.5.0 RESEARCH.md snippet):
      boolean canHarvest = tool.isCorrectToolForDrops(state) || !state.requiresCorrectToolForDrops();
      float divisor = canHarvest ? 30.0F : 100.0F;
      float levelBonus = 1.0F + (s.getSquireLevel() * SquireConfig.miningSpeedPerLevel.get().floatValue());
      float progressPerTick = (toolSpeed / (destroySpeed * divisor)) * SquireConfig.breakSpeedMultiplier.get().floatValue() * levelBonus;
  </behavior>
  <action>
    Port v0.5.0 MiningHandler with these v2 changes:
    - Replace squire.getSquireInventory() with squire.getItemHandler()
    - Replace direct inventory mutation with insertItem(slot, stack, false); overflow → squire.spawnAtLocation(overflow)
    - Replace squire.getSquireAI() references with squire.getBrain() (method name changed in v2)
    - Replace SquireAbilities ability checks with squire.getSquireLevel() >= 0 (mining is unlocked from Servant tier; no gate needed)
    - Ensure no hardcoded block instance checks — break any block the squire can reach
    - Package: com.sjviklabs.squire.brain.handler
    - Constructor: MiningHandler(SquireEntity squire, TickRateStateMachine machine)
    - Public API: void setTarget(BlockPos), void setAreaTarget(BlockPos cornerA, BlockPos cornerB), void tick(SquireAIState currentState)
    - Tool slot: scan equipment slots 0-5 of getItemHandler() for best pickaxe/shovel using item.getDestroySpeed(state) comparison

    After writing MiningHandler, run ./gradlew test — MiningHandlerTest MUST go GREEN.
  </action>
  <verify>
    <automated>cd C:/Users/Steve/Projects/squire-mod-v2 && ./gradlew test --tests "com.sjviklabs.squire.brain.handler.MiningHandlerTest" 2>&1 | tail -20</automated>
  </verify>
  <done>MiningHandlerTest all assertions pass (GREEN). ./gradlew compileJava exits 0. MiningHandlerGameTest stub still compiles (helper.succeed() only).</done>
</task>

</tasks>

<verification>
Full suite check after all three tasks:

```bash
cd C:/Users/Steve/Projects/squire-mod-v2 && ./gradlew test
```

Expected: TorchHandlerTest + MiningHandlerTest green. All Phase 1 tests still green. Zero compile errors.
</verification>

<success_criteria>
- TorchHandler exists in brain/handler/, uses IItemHandler API (not SimpleContainer), is NOT a FSM state
- MiningHandler exists in brain/handler/, handles both single-block and area-clear flows, calls ensureChunkLoaded() before area queue build
- Break speed formula matches RESEARCH.md snippet exactly (divisor 30/100, level bonus from config)
- All 5 unit test behaviors in TorchHandlerTest pass
- All 4 unit test behaviors in MiningHandlerTest pass
- MiningHandlerGameTest compiles with stub (helper.succeed())
- ./gradlew test exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/06-work-behaviors/06-01-SUMMARY.md` following the summary template.
</output>
