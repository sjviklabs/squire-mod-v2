---
phase: 06-work-behaviors
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/com/sjviklabs/squire/brain/handler/FarmingHandler.java
  - src/main/java/com/sjviklabs/squire/brain/handler/FishingHandler.java
  - src/test/java/com/sjviklabs/squire/brain/handler/FishingHandlerTest.java
  - src/test/java/com/sjviklabs/squire/gametest/FarmingHandlerGameTest.java
autonomous: true
requirements: [WRK-04, WRK-05]

must_haves:
  truths:
    - "Squire tills dirt adjacent to water, plants seeds from inventory, harvests mature crops, replants automatically"
    - "Squire fishes using the vanilla minecraft:gameplay/fishing loot table (correct probabilities, mopack-overridable)"
    - "Farming scan uses CropBlock.isMaxAge(state) — no hardcoded block type lists"
    - "Fishing loot is inserted into squire inventory; overflow dropped at feet"
  artifacts:
    - path: "src/main/java/com/sjviklabs/squire/brain/handler/FarmingHandler.java"
      provides: "FSM FARM_SCAN → FARM_APPROACH → FARM_WORK cycle"
      contains: "class FarmingHandler"
    - path: "src/main/java/com/sjviklabs/squire/brain/handler/FishingHandler.java"
      provides: "FSM FISHING_APPROACH → FISHING_IDLE simulated fishing via loot table"
      contains: "class FishingHandler"
    - path: "src/test/java/com/sjviklabs/squire/brain/handler/FishingHandlerTest.java"
      provides: "Unit tests: loot roll produces valid items, cod is most common over N trials"
      contains: "class FishingHandlerTest"
    - path: "src/test/java/com/sjviklabs/squire/gametest/FarmingHandlerGameTest.java"
      provides: "GameTest stub: till/plant/harvest cycle"
      contains: "class FarmingHandlerGameTest"
  key_links:
    - from: "FarmingHandler.performScan()"
      to: "CropBlock.isMaxAge(state)"
      via: "instanceof CropBlock check on BlockState"
      pattern: "CropBlock.*isMaxAge"
    - from: "FishingHandler.rollFishingLoot()"
      to: "LootTable.getRandomItems(params)"
      via: "LootContextParamSets.FISHING with ORIGIN + TOOL params"
      pattern: "LootContextParamSets\\.FISHING"
    - from: "FishingHandler tickBreak deposits"
      to: "squire.getItemHandler().insertItem()"
      via: "IItemHandler for inventory deposit"
      pattern: "getItemHandler\\(\\)\\.insertItem"
---

<objective>
Implement FarmingHandler (FARM_SCAN → FARM_APPROACH → FARM_WORK cycle with CropBlock-based detection) and FishingHandler (simulated fishing via the vanilla loot table). Runs parallel to Plan 06-01 — no file overlap.

Purpose: Squire can work a farm autonomously and fish at a marked location. FishingHandler fixes the v0.5.0 loot rate bug by using LootContextParamSets.FISHING.
Output: FarmingHandler, FishingHandler, FishingHandlerTest (unit), FarmingHandlerGameTest (stub).
</objective>

<execution_context>
@C:/Users/Steve/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Steve/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/Steve/Projects/squire-mod-v2/.planning/PROJECT.md
@C:/Users/Steve/Projects/squire-mod-v2/.planning/ROADMAP.md
@C:/Users/Steve/Projects/squire-mod-v2/.planning/phases/06-work-behaviors/06-CONTEXT.md
@C:/Users/Steve/Projects/squire-mod-v2/.planning/phases/06-work-behaviors/06-RESEARCH.md

<!-- v0.5.0 reference (read-only — note the crop type bug described in RESEARCH.md open question 3) -->
@C:/Users/Steve/Projects/squire-mod/src/main/java/com/sjviklabs/squire/ai/handler/FarmingHandler.java
@C:/Users/Steve/Projects/squire-mod/src/main/java/com/sjviklabs/squire/ai/handler/FishingHandler.java

<interfaces>
<!-- Key contracts the executor needs. No codebase exploration required. -->

From src/main/java/com/sjviklabs/squire/brain/SquireAIState.java (Phase 2):
  IDLE, FOLLOWING_OWNER,
  FARM_SCAN, FARM_APPROACH, FARM_WORK,
  FISHING_APPROACH, FISHING_IDLE

From src/main/java/com/sjviklabs/squire/entity/SquireEntity.java (Phase 1):
  public SquireItemHandler getItemHandler()
  public int getSquireLevel()
  public void spawnAtLocation(ItemStack stack)     // drops item in world at feet

From src/main/java/com/sjviklabs/squire/brain/TickRateStateMachine.java (Phase 2):
  public void forceState(SquireAIState state)
  public SquireAIState getState()

From src/main/java/com/sjviklabs/squire/brain/SquireBrainEventBus.java (Phase 2):
  public void fire(SquireEvent event, SquireEntity squire)

From src/main/java/com/sjviklabs/squire/config/SquireConfig.java (Phase 1):
  // Farming section:
  public static ForgeConfigSpec.IntValue farmingReach;         // default 5
  public static ForgeConfigSpec.IntValue farmWorkCooldown;     // ticks between actions
  // Fishing section:
  public static ForgeConfigSpec.IntValue fishingDuration;      // ticks per catch, default 200
  public static ForgeConfigSpec.IntValue fishingReach;         // approach range

NeoForge 1.21.1 loot API (verified in RESEARCH.md):
  LootTable table = level.getServer().reloadableRegistries().getLootTable(BuiltInLootTables.FISHING);
  LootParams params = new LootParams.Builder(level)
      .withParameter(LootContextParams.ORIGIN, squire.position())
      .withParameter(LootContextParams.TOOL, rod)
      .withOptionalParameter(LootContextParams.THIS_ENTITY, squire)
      .create(LootContextParamSets.FISHING);
  List<ItemStack> drops = table.getRandomItems(params);
  // Note: verify BuiltInLootTables.FISHING constant name against actual NeoForge 21.1.x source at compile time
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test scaffolds — FishingHandlerTest, FarmingHandlerGameTest</name>
  <files>
    src/test/java/com/sjviklabs/squire/brain/handler/FishingHandlerTest.java
    src/test/java/com/sjviklabs/squire/gametest/FarmingHandlerGameTest.java
  </files>
  <read_first>
    - src/test/java/com/sjviklabs/squire/inventory/SquireItemHandlerTest.java (test double pattern)
    - src/test/java/com/sjviklabs/squire/gametest/SquireEntityGameTest.java (@GameTestHolder annotation)
    - C:/Users/Steve/Projects/squire-mod-v2/.planning/phases/06-work-behaviors/06-RESEARCH.md (WRK-04 and WRK-05 test map, pitfalls 1 and 2)
  </read_first>
  <behavior>
    FishingHandlerTest (unit — must mock ServerLevel for loot roll):
    - test_noRod_returnsEmpty: squire inventory has no fishing rod → rollFishingLoot() returns empty list
    - test_withRod_returnsNonEmpty: mock loot roll returns at least 1 item → result list not empty
    - test_lootTablePath_isFISHING: verify that rollFishingLoot() constructs params with LootContextParamSets.FISHING (not EMPTY); assert IllegalStateException is NOT thrown when params are built with ORIGIN + TOOL

    FarmingHandlerGameTest (GameTest stub — compiles, calls helper.succeed()):
    - test_tillPlantHarvest: TODO Phase 6 execution — stub for now
  </behavior>
  <action>
    FishingHandlerTest: The loot roll test requires a ServerLevel. Use Mockito (it is on the test classpath via NeoForge moddev unitTest) to mock ServerLevel. For test_withRod_returnsNonEmpty, call rollFishingLoot() via a TestableFishingHandler inner class that accepts a mock ServerLevel. The test double should expose rollFishingLoot() as package-private for testing. For test_lootTablePath_isFISHING: build a LootParams object directly in test using LootContextParamSets.FISHING, supply a Vec3 for ORIGIN and an ItemStack for TOOL; confirm create() does not throw — this proves the param set accepts ORIGIN + TOOL.

    If Mockito is not on classpath, check build.gradle testImplementation section. If absent, add:
      testImplementation "org.mockito:mockito-core:5.11.0"
    inside the dependencies block (not inside neoForge {}). Run ./gradlew compileTestJava to confirm.

    FarmingHandlerGameTest: @GameTestHolder(value = "squire"), @PrefixGameTestTemplate(false), template "squire:empty", single test method test_tillPlantHarvest → helper.succeed() with TODO comment.
  </action>
  <verify>
    <automated>cd C:/Users/Steve/Projects/squire-mod-v2 && ./gradlew compileTestJava 2>&1 | tail -20</automated>
  </verify>
  <done>Both test files compile. FishingHandlerTest fails at runtime (RED — FishingHandler class not yet created). FarmingHandlerGameTest compiles as a stub.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FarmingHandler — FARM_SCAN → FARM_APPROACH → FARM_WORK cycle</name>
  <files>
    src/main/java/com/sjviklabs/squire/brain/handler/FarmingHandler.java
  </files>
  <read_first>
    - C:/Users/Steve/Projects/squire-mod/src/main/java/com/sjviklabs/squire/ai/handler/FarmingHandler.java (v0.5.0 reference)
    - C:/Users/Steve/Projects/squire-mod-v2/.planning/phases/06-work-behaviors/06-RESEARCH.md (pitfall 5 — Y-level contract, open question 3 — crop extensibility)
    - src/main/java/com/sjviklabs/squire/config/SquireConfig.java (farmingReach, farmWorkCooldown)
  </read_first>
  <behavior>
    - setArea(BlockPos cornerA, BlockPos cornerB): stores area bounds; cornerA Y is the farmland/ground level (document this contract in a Javadoc comment)
    - FARM_SCAN: scan all (x, cornerA.y, z) positions in area; collect positions where blockstate is FarmBlock AND the block above is either: (a) a CropBlock at isMaxAge(state) [harvest], or (b) air and squire has a seed in inventory [plant after till]; add to workQueue
    - FARM_APPROACH: pathfind to nearest workQueue entry within farmingReach; stuck detection (same pattern as MiningHandler)
    - FARM_WORK: if harvest target → break crop (level.destroyBlock), collect drops via insertItem; if till+plant target → use hoe from equipment slots to till (Blocks.FARMLAND.defaultBlockState()), then place seed above; replant flag: after harvest, add same position back to workQueue for planting if seed available
    - Seed detection: use item tag check Items.getItem(item).getItem() instanceof BlockItem && ((BlockItem)item).getBlock() instanceof CropBlock — do NOT hardcode wheat/potato/carrot/beetroot list. Create item tag squire:plantable_seeds in data/squire/tags/items/ as a JSON file listing vanilla seeds; external modded seeds discovered via CropBlock instanceof check.
    - On area complete → machine.forceState(IDLE), fire SquireEvent.WORK_TASK_COMPLETE
  </behavior>
  <action>
    Port v0.5.0 FarmingHandler with these v2 changes:
    - Replace hardcoded isSeedItem() / getCropForSeed() switch with CropBlock instanceof check (see RESEARCH.md open question 3)
    - Create src/main/resources/data/squire/tags/items/plantable_seeds.json listing vanilla seeds: minecraft:wheat_seeds, minecraft:potato, minecraft:carrot, minecraft:beetroot_seeds, minecraft:melon_seeds, minecraft:pumpkin_seeds
    - Replace squire.getSquireInventory() with squire.getItemHandler()
    - Replace SquireAbilities.hasFarming() with squire.getSquireLevel() >= 0 (farming available from Servant)
    - Package: com.sjviklabs.squire.brain.handler
    - Constructor: FarmingHandler(SquireEntity squire, TickRateStateMachine machine)
    - Public API: void setArea(BlockPos cornerA, BlockPos cornerB), void tick(SquireAIState currentState)
    - Activity log: if (SquireConfig.activityLogging.get()) LOGGER.debug(...)
  </action>
  <verify>
    <automated>cd C:/Users/Steve/Projects/squire-mod-v2 && ./gradlew compileJava 2>&1 | tail -20</automated>
  </verify>
  <done>FarmingHandler compiles. ./gradlew compileJava exits 0. No FarmingHandlerTest unit tests exist for this plan (FarmingHandler behavior is GameTest-verified in WRK-04 test map; unit tests are not required here).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: FishingHandler — simulated fishing via vanilla loot table</name>
  <files>
    src/main/java/com/sjviklabs/squire/brain/handler/FishingHandler.java
  </files>
  <read_first>
    - C:/Users/Steve/Projects/squire-mod/src/main/java/com/sjviklabs/squire/ai/handler/FishingHandler.java (v0.5.0 reference — note hardcoded loot bug)
    - C:/Users/Steve/Projects/squire-mod-v2/.planning/phases/06-work-behaviors/06-RESEARCH.md (FishingHandler loot code example, pitfalls 1 and 2)
    - src/main/java/com/sjviklabs/squire/config/SquireConfig.java (fishingDuration, fishingReach)
  </read_first>
  <behavior>
    - startFishing(): stores water target position, calls machine.forceState(FISHING_APPROACH)
    - FISHING_APPROACH: pathfind to within fishingReach of water target; stuck detection; on arrival → machine.forceState(FISHING_IDLE), reset fishingTimer = 0
    - FISHING_IDLE: increment fishingTimer each tick; play fishing animation if available (no-op if animation not wired yet); on fishingTimer >= SquireConfig.fishingDuration.get() → call rollFishingLoot(); insert each result via getItemHandler().insertItem(); overflow → spawnAtLocation(); reset fishingTimer; repeat (keep fishing in FISHING_IDLE) unless task queue pops next task
    - Requires fishing rod in equipment slots (scan slots 0-5 for Items.FISHING_ROD); if no rod → log warning, return to IDLE immediately
    - rollFishingLoot(): use code from RESEARCH.md code examples section exactly — BuiltInLootTables.FISHING, LootContextParamSets.FISHING, withParameter ORIGIN and TOOL
    - stopFishing() / task complete: called by TaskQueue dispatch when next task arrives → machine.forceState(IDLE), fire SquireEvent.WORK_TASK_COMPLETE
  </behavior>
  <action>
    Port v0.5.0 FishingHandler replacing the hardcoded probability table with the loot table approach from RESEARCH.md.

    Critical: Do NOT use LootContextParamSets.EMPTY. Always use LootContextParamSets.FISHING.

    For BuiltInLootTables.FISHING: use that constant name; if it does not compile (tertiary confidence from RESEARCH.md), fall back to ResourceLocation.fromNamespaceAndPath("minecraft", "gameplay/fishing") and leave a comment: "// BuiltInLootTables.FISHING constant not found — using explicit ResourceLocation per RESEARCH.md tertiary note".

    The rollFishingLoot() method must be package-private (not private) so FishingHandlerTest can access it via the TestableFishingHandler inner class.

    Package: com.sjviklabs.squire.brain.handler
    Constructor: FishingHandler(SquireEntity squire, TickRateStateMachine machine)
    Public API: void startFishing(), void stopFishing(), void tick(SquireAIState currentState)

    After writing FishingHandler, run ./gradlew test — FishingHandlerTest MUST go GREEN.
  </action>
  <verify>
    <automated>cd C:/Users/Steve/Projects/squire-mod-v2 && ./gradlew test --tests "com.sjviklabs.squire.brain.handler.FishingHandlerTest" 2>&1 | tail -20</automated>
  </verify>
  <done>FishingHandlerTest all assertions pass (GREEN). ./gradlew compileJava exits 0. Loot roll uses LootContextParamSets.FISHING — not EMPTY, not a hardcoded table.</done>
</task>

</tasks>

<verification>
Full suite check after all three tasks:

```bash
cd C:/Users/Steve/Projects/squire-mod-v2 && ./gradlew test
```

Expected: FishingHandlerTest green. All Phase 1 tests still green. Zero compile errors.
</verification>

<success_criteria>
- FarmingHandler exists in brain/handler/, uses CropBlock.isMaxAge() (no hardcoded crop list), uses IItemHandler API
- plantable_seeds.json tag file exists at data/squire/tags/items/plantable_seeds.json with 6 vanilla seed entries
- FishingHandler exists in brain/handler/, rollFishingLoot() uses LootContextParamSets.FISHING
- FishingHandlerTest GREEN (3 assertions)
- FarmingHandlerGameTest compiles as stub
- ./gradlew test exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/06-work-behaviors/06-02-SUMMARY.md` following the summary template.
</output>
