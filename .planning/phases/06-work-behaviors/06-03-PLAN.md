---
phase: 06-work-behaviors
plan: "03"
type: execute
wave: 2
depends_on: ["06-01", "06-02"]
files_modified:
  - src/main/java/com/sjviklabs/squire/brain/handler/PlacingHandler.java
  - src/main/java/com/sjviklabs/squire/brain/handler/ChestHandler.java
  - src/test/java/com/sjviklabs/squire/gametest/PlacingHandlerGameTest.java
  - src/test/java/com/sjviklabs/squire/gametest/ChestHandlerGameTest.java
autonomous: true
requirements: [WRK-06, WRK-07]

must_haves:
  truths:
    - "Squire places a targeted block from its inventory at a specified position on command"
    - "Squire deposits excess inventory items into a target chest, and withdraws requested items from a chest"
    - "Chest interaction works with both vanilla chests (BaseContainerBlockEntity) and modded storage (IItemHandler capability fallback)"
    - "PlacingHandler caches the inventory slot index at setTarget() time — no linear scan every tick"
  artifacts:
    - path: "src/main/java/com/sjviklabs/squire/brain/handler/PlacingHandler.java"
      provides: "FSM PLACING_APPROACH → PLACING_BLOCK cycle with slot caching"
      contains: "class PlacingHandler"
    - path: "src/main/java/com/sjviklabs/squire/brain/handler/ChestHandler.java"
      provides: "FSM CHEST_APPROACH → CHEST_INTERACT with dual-path container support"
      contains: "class ChestHandler"
    - path: "src/test/java/com/sjviklabs/squire/gametest/PlacingHandlerGameTest.java"
      provides: "GameTest stub: block placed, item consumed from inventory"
      contains: "class PlacingHandlerGameTest"
    - path: "src/test/java/com/sjviklabs/squire/gametest/ChestHandlerGameTest.java"
      provides: "GameTest stub: chest deposit moves items from squire to chest"
      contains: "class ChestHandlerGameTest"
  key_links:
    - from: "PlacingHandler.setTarget()"
      to: "squire.getItemHandler() slot scan"
      via: "cache inventorySlot index at call time (not per tick)"
      pattern: "inventorySlot\\s*="
    - from: "ChestHandler.tickInteract()"
      to: "BaseContainerBlockEntity (vanilla path)"
      via: "instanceof check first"
      pattern: "instanceof BaseContainerBlockEntity"
    - from: "ChestHandler.tickInteract()"
      to: "Capabilities.ItemHandler.BLOCK (modded path)"
      via: "level.getCapability() fallback after vanilla check fails"
      pattern: "Capabilities\\.ItemHandler\\.BLOCK"
---

<objective>
Implement PlacingHandler (single-block placement from inventory) and ChestHandler (container deposit/withdraw with vanilla + modded fallback). Both port cleanly from v0.5.0 with IItemHandler API substitution.

Purpose: Squire can place blocks on command and interact with storage containers — completing the non-combat work repertoire alongside mining and farming.
Output: PlacingHandler, ChestHandler, 2 GameTest stubs.
</objective>

<execution_context>
@C:/Users/Steve/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Steve/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/Steve/Projects/squire-mod-v2/.planning/PROJECT.md
@C:/Users/Steve/Projects/squire-mod-v2/.planning/ROADMAP.md
@C:/Users/Steve/Projects/squire-mod-v2/.planning/phases/06-work-behaviors/06-CONTEXT.md
@C:/Users/Steve/Projects/squire-mod-v2/.planning/phases/06-work-behaviors/06-RESEARCH.md

<!-- Prior plan outputs from this phase — handler pattern established here -->
@C:/Users/Steve/Projects/squire-mod-v2/.planning/phases/06-work-behaviors/06-01-SUMMARY.md
@C:/Users/Steve/Projects/squire-mod-v2/.planning/phases/06-work-behaviors/06-02-SUMMARY.md

<!-- v0.5.0 reference (read-only) -->
@C:/Users/Steve/Projects/squire-mod/src/main/java/com/sjviklabs/squire/ai/handler/PlacingHandler.java
@C:/Users/Steve/Projects/squire-mod/src/main/java/com/sjviklabs/squire/ai/handler/ChestHandler.java

<interfaces>
<!-- Key contracts the executor needs. No codebase exploration required. -->

From src/main/java/com/sjviklabs/squire/brain/SquireAIState.java (Phase 2):
  IDLE,
  PLACING_APPROACH, PLACING_BLOCK,
  CHEST_APPROACH, CHEST_INTERACT

From src/main/java/com/sjviklabs/squire/entity/SquireEntity.java (Phase 1):
  public SquireItemHandler getItemHandler()
  public void spawnAtLocation(ItemStack stack)

From src/main/java/com/sjviklabs/squire/brain/TickRateStateMachine.java (Phase 2):
  public void forceState(SquireAIState state)

From src/main/java/com/sjviklabs/squire/brain/SquireBrainEventBus.java (Phase 2):
  public void fire(SquireEvent event, SquireEntity squire)

From src/main/java/com/sjviklabs/squire/config/SquireConfig.java (Phase 1):
  public static ForgeConfigSpec.IntValue placingReach;     // default 4
  public static ForgeConfigSpec.IntValue chestReach;       // default 3
  public static ForgeConfigSpec.IntValue chestInteractCooldown; // ticks between transfers

NeoForge 1.21.1 container capability (from RESEARCH.md ChestHandler code example):
  // Dual-path: vanilla first, modded fallback
  var blockEntity = level.getBlockEntity(targetPos);
  if (blockEntity instanceof BaseContainerBlockEntity container) {
      depositItems(squire, container);
  } else {
      IItemHandler handler = level.getCapability(Capabilities.ItemHandler.BLOCK, targetPos, null);
      if (handler != null) { depositItemsHandler(squire, handler); }
  }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GameTest stubs — PlacingHandlerGameTest, ChestHandlerGameTest</name>
  <files>
    src/test/java/com/sjviklabs/squire/gametest/PlacingHandlerGameTest.java
    src/test/java/com/sjviklabs/squire/gametest/ChestHandlerGameTest.java
  </files>
  <read_first>
    - src/test/java/com/sjviklabs/squire/gametest/SquireEntityGameTest.java (@GameTestHolder pattern)
    - src/test/java/com/sjviklabs/squire/gametest/MiningHandlerGameTest.java (stub pattern from 06-01)
    - C:/Users/Steve/Projects/squire-mod-v2/.planning/phases/06-work-behaviors/06-RESEARCH.md (WRK-06 and WRK-07 test map)
  </read_first>
  <action>
    Create two GameTest stub files following the exact pattern from MiningHandlerGameTest (06-01):
    - @GameTestHolder(value = "squire"), @PrefixGameTestTemplate(false), template "squire:empty"
    - PlacingHandlerGameTest: single test method test_blockPlaced_itemConsumed → helper.succeed() with TODO
    - ChestHandlerGameTest: single test method test_chestDeposit_movesItemsFromSquire → helper.succeed() with TODO
    Both stubs compile and execute as no-ops. No behavior to verify yet.
  </action>
  <verify>
    <automated>cd C:/Users/Steve/Projects/squire-mod-v2 && ./gradlew compileTestJava 2>&1 | tail -10</automated>
  </verify>
  <done>Both files compile. ./gradlew compileTestJava exits 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PlacingHandler — PLACING_APPROACH → PLACING_BLOCK with inventory slot caching</name>
  <files>
    src/main/java/com/sjviklabs/squire/brain/handler/PlacingHandler.java
  </files>
  <read_first>
    - C:/Users/Steve/Projects/squire-mod/src/main/java/com/sjviklabs/squire/ai/handler/PlacingHandler.java (v0.5.0 reference)
    - src/main/java/com/sjviklabs/squire/brain/handler/MiningHandler.java (approach/stuck pattern established in 06-01)
    - src/main/java/com/sjviklabs/squire/config/SquireConfig.java (placingReach)
  </read_first>
  <behavior>
    - setTarget(BlockPos targetPos, Item blockItem): scan squire inventory for blockItem, cache slot index as inventorySlot field; if not found → log warning, return without state change; if found → machine.forceState(PLACING_APPROACH)
    - PLACING_APPROACH: pathfind to within placingReach of targetPos; stuck detection (same STUCK_TIMEOUT=100, MAX_REPOSITION_ATTEMPTS=3 constants); on arrival → machine.forceState(PLACING_BLOCK)
    - PLACING_BLOCK: confirm targetPos is air (level.isEmptyBlock(targetPos)); confirm squire still has blockItem in cached slot (re-validate); call level.setBlock(targetPos, block.defaultBlockState(), 3); call getItemHandler().extractItem(inventorySlot, 1, false); machine.forceState(IDLE); fire SquireEvent.WORK_TASK_COMPLETE
    - If target is occupied (block already there) → machine.forceState(IDLE), log "target occupied", fire WORK_TASK_COMPLETE (task complete — block is placed, even if not by squire)
  </behavior>
  <action>
    Port v0.5.0 PlacingHandler with these v2 changes:
    - Replace squire.getSquireInventory() with squire.getItemHandler()
    - Replace direct item mutation with extractItem(inventorySlot, 1, false)
    - Keep the slot-caching optimization from v0.5.0 (scan once in setTarget, cache index)
    - Package: com.sjviklabs.squire.brain.handler
    - Constructor: PlacingHandler(SquireEntity squire, TickRateStateMachine machine)
    - Public API: void setTarget(BlockPos pos, Item blockItem), void tick(SquireAIState currentState)
    - No unit tests in this plan — PlacingHandler is GameTest-verified (WRK-06). Compile verification only.
  </action>
  <verify>
    <automated>cd C:/Users/Steve/Projects/squire-mod-v2 && ./gradlew compileJava 2>&1 | tail -10</automated>
  </verify>
  <done>PlacingHandler compiles. ./gradlew compileJava exits 0. Slot caching: inventorySlot set in setTarget(), not recomputed on every tick.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ChestHandler — CHEST_APPROACH → CHEST_INTERACT with dual-path container support</name>
  <files>
    src/main/java/com/sjviklabs/squire/brain/handler/ChestHandler.java
  </files>
  <read_first>
    - C:/Users/Steve/Projects/squire-mod/src/main/java/com/sjviklabs/squire/ai/handler/ChestHandler.java (v0.5.0 reference)
    - C:/Users/Steve/Projects/squire-mod-v2/.planning/phases/06-work-behaviors/06-RESEARCH.md (pitfall 6 — modded storage fallback, ChestHandler code example)
    - src/main/java/com/sjviklabs/squire/config/SquireConfig.java (chestReach, chestInteractCooldown)
  </read_first>
  <behavior>
    - setDepositTarget(BlockPos chestPos): stores target, mode = DEPOSIT, machine.forceState(CHEST_APPROACH)
    - setWithdrawTarget(BlockPos chestPos, Item requestedItem, int amount): stores target + request, mode = WITHDRAW, machine.forceState(CHEST_APPROACH)
    - CHEST_APPROACH: pathfind to within chestReach blocks; stuck detection; on arrival → machine.forceState(CHEST_INTERACT)
    - CHEST_INTERACT deposit: transfer items from squire backpack slots (slots 6+) to container until container full or squire empty; use dual-path: BaseContainerBlockEntity first, then Capabilities.ItemHandler.BLOCK fallback (see code example in RESEARCH.md and context above)
    - CHEST_INTERACT withdraw: search container for requestedItem; extract up to requested amount via insertItem into squire inventory; overflow dropped at feet
    - On complete → machine.forceState(IDLE), fire SquireEvent.WORK_TASK_COMPLETE
    - Ability gate: squire.getSquireLevel() >= SquireConfig.chestAbilityMinLevel.get() (add config key if absent; fallback: tier ordinal >= 2 meaning Knight or higher; add TODO for Phase 4 ability system)
  </behavior>
  <action>
    Port v0.5.0 ChestHandler with these v2 changes:
    - Replace squire.getSquireInventory() with squire.getItemHandler()
    - Keep the dual-path container interaction (BaseContainerBlockEntity + IItemHandler fallback) verbatim — this is the correct pattern for modded storage (see pitfall 6)
    - Replace SquireAbilities.hasChestDeposit() with level-gate as described in behavior spec above
    - Package: com.sjviklabs.squire.brain.handler
    - Constructor: ChestHandler(SquireEntity squire, TickRateStateMachine machine)
    - Public API: void setDepositTarget(BlockPos pos), void setWithdrawTarget(BlockPos pos, Item item, int amount), void tick(SquireAIState currentState)
    - No unit tests in this plan — ChestHandler is GameTest-verified (WRK-07). Compile verification only.
  </action>
  <verify>
    <automated>cd C:/Users/Steve/Projects/squire-mod-v2 && ./gradlew test 2>&1 | tail -20</automated>
  </verify>
  <done>ChestHandler compiles. ./gradlew test exits 0. Dual-path container logic present: BaseContainerBlockEntity check followed by Capabilities.ItemHandler.BLOCK fallback.</done>
</task>

</tasks>

<verification>
Full suite after all tasks:

```bash
cd C:/Users/Steve/Projects/squire-mod-v2 && ./gradlew test
```

Expected: All Phase 1 tests + all Phase 6 unit tests (TorchHandler, MiningHandler, FishingHandler) still green. PlacingHandler and ChestHandler compile. GameTest stubs compile.
</verification>

<success_criteria>
- PlacingHandler exists in brain/handler/, slot scan in setTarget() (not per tick), uses extractItem()
- ChestHandler exists in brain/handler/, dual-path: BaseContainerBlockEntity then Capabilities.ItemHandler.BLOCK fallback
- Both GameTest stubs compile under squire namespace
- ./gradlew test exits 0 (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/06-work-behaviors/06-03-SUMMARY.md` following the summary template.
</output>
