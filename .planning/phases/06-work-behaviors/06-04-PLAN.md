---
phase: 06-work-behaviors
plan: "04"
type: execute
wave: 3
depends_on: ["06-01", "06-02", "06-03"]
files_modified:
  - src/main/java/com/sjviklabs/squire/brain/TaskQueue.java
  - src/main/java/com/sjviklabs/squire/brain/handler/ChatHandler.java
  - src/main/java/com/sjviklabs/squire/entity/SquireEntity.java
  - src/main/java/com/sjviklabs/squire/brain/SquireBrain.java
  - src/test/java/com/sjviklabs/squire/brain/TaskQueueTest.java
autonomous: true
requirements: [WRK-08]

must_haves:
  truths:
    - "Player issues multiple work commands in sequence; squire executes them in order and reports completion via chat"
    - "TaskQueue persists across server restart (NBT round-trip in SquireEntity save/load methods)"
    - "ChatHandler sends tier-appropriate personality lines on state transitions (idle, combat, level-up, new tier)"
    - "SquireBrain dispatch: on IDLE with non-empty queue, pops next task and routes to the correct handler"
  artifacts:
    - path: "src/main/java/com/sjviklabs/squire/brain/TaskQueue.java"
      provides: "ArrayDeque-backed FIFO task queue with NBT persistence"
      contains: "class TaskQueue"
    - path: "src/main/java/com/sjviklabs/squire/brain/handler/ChatHandler.java"
      provides: "Event subscriber sending personality chat lines per tier and state"
      contains: "class ChatHandler"
    - path: "src/test/java/com/sjviklabs/squire/brain/TaskQueueTest.java"
      provides: "Unit tests: FIFO order, NBT round-trip, max-length enforcement"
      contains: "class TaskQueueTest"
  key_links:
    - from: "SquireEntity.addAdditionalSaveData()"
      to: "squireBrain.getTaskQueue().save()"
      via: "tag.put(\"taskQueue\", ...)"
      pattern: "taskQueue.*save\\(\\)"
    - from: "SquireEntity.readAdditionalSaveData()"
      to: "squireBrain.getTaskQueue().load()"
      via: "tag.contains(\"taskQueue\") guard"
      pattern: "taskQueue.*load\\(\\)"
    - from: "SquireBrain.tick() IDLE branch"
      to: "taskQueue.poll() → handler dispatch"
      via: "switch on SquireTask.commandName()"
      pattern: "taskQueue\\.poll\\(\\)"
    - from: "SquireBrainEventBus WORK_TASK_COMPLETE"
      to: "ChatHandler.onWorkComplete()"
      via: "event subscription"
      pattern: "onWorkComplete"
---

<objective>
Wire the TaskQueue into SquireBrain (dispatch on IDLE) and SquireEntity (NBT persistence), then implement ChatHandler with tier-based personality lines. This completes Phase 6 — after this plan, the squire queues and executes multiple work commands sequentially and reports back to the player.

Purpose: Task queuing is the glue that makes all Phase 6 handlers feel like a system rather than isolated commands. ChatHandler gives the squire its voice.
Output: TaskQueue (with tests), ChatHandler, SquireBrain and SquireEntity wired.
</objective>

<execution_context>
@C:/Users/Steve/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Steve/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/Steve/Projects/squire-mod-v2/.planning/PROJECT.md
@C:/Users/Steve/Projects/squire-mod-v2/.planning/ROADMAP.md
@C:/Users/Steve/Projects/squire-mod-v2/.planning/phases/06-work-behaviors/06-CONTEXT.md
@C:/Users/Steve/Projects/squire-mod-v2/.planning/phases/06-work-behaviors/06-RESEARCH.md

<!-- Prior plan summaries for this phase -->
@C:/Users/Steve/Projects/squire-mod-v2/.planning/phases/06-work-behaviors/06-01-SUMMARY.md
@C:/Users/Steve/Projects/squire-mod-v2/.planning/phases/06-work-behaviors/06-02-SUMMARY.md
@C:/Users/Steve/Projects/squire-mod-v2/.planning/phases/06-work-behaviors/06-03-SUMMARY.md

<!-- v0.5.0 reference (read-only) -->
@C:/Users/Steve/Projects/squire-mod/src/main/java/com/sjviklabs/squire/util/TaskQueue.java
@C:/Users/Steve/Projects/squire-mod/src/main/java/com/sjviklabs/squire/ai/handler/ChatHandler.java

<interfaces>
<!-- Key contracts the executor needs. No codebase exploration required. -->

From src/main/java/com/sjviklabs/squire/brain/SquireAIState.java (Phase 2):
  IDLE, FOLLOWING_OWNER,
  MINING_APPROACH, PLACING_APPROACH,
  FARM_SCAN, FISHING_APPROACH, CHEST_APPROACH

From src/main/java/com/sjviklabs/squire/entity/SquireEntity.java (Phase 1/current):
  public SquireBrain getBrain()            // access to SquireBrain instance
  // NBT hooks already exist:
  public void addAdditionalSaveData(CompoundTag tag)   // add tag.put("taskQueue", ...) here
  public void readAdditionalSaveData(CompoundTag tag)  // load taskQueue here

From src/main/java/com/sjviklabs/squire/brain/SquireBrain.java (Phase 2):
  // Handlers already instantiated in SquireBrain constructor (Phase 2-3 wiring)
  // Add: TaskQueue taskQueue field
  // Add: getTaskQueue() accessor
  // Add: dispatch logic in tick() IDLE branch

From src/main/java/com/sjviklabs/squire/brain/SquireBrainEventBus.java (Phase 2):
  public void fire(SquireEvent event, SquireEntity squire)
  // Subscribe: bus.subscribe(SquireEvent.WORK_TASK_COMPLETE, chatHandler::onWorkComplete)
  // Subscribe: bus.subscribe(SquireEvent.STATE_CHANGED, chatHandler::onStateChange)

From src/main/java/com/sjviklabs/squire/config/SquireConfig.java (Phase 1):
  public static ForgeConfigSpec.IntValue chatCooldownTicks;   // default 600 (30s) — spam prevention
  public static ForgeConfigSpec.BooleanValue enablePersonalityChat;  // default true

SquireTask record (define inline in TaskQueue.java or as a top-level record):
  record SquireTask(String commandName, CompoundTag params) {
      public static SquireTask fromNBT(CompoundTag tag) { ... }
      public CompoundTag toNBT() { ... }
  }
  // commandName values: "mine", "mine_area", "place", "farm", "fish", "chest_deposit", "chest_withdraw"
  // params: BlockPos, Item, area corners etc. stored as NBT compound
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TaskQueue — FIFO queue with NBT persistence, then wire into SquireBrain + SquireEntity</name>
  <files>
    src/main/java/com/sjviklabs/squire/brain/TaskQueue.java
    src/main/java/com/sjviklabs/squire/brain/SquireBrain.java
    src/main/java/com/sjviklabs/squire/entity/SquireEntity.java
    src/test/java/com/sjviklabs/squire/brain/TaskQueueTest.java
  </files>
  <read_first>
    - C:/Users/Steve/Projects/squire-mod/src/main/java/com/sjviklabs/squire/util/TaskQueue.java (v0.5.0 reference)
    - C:/Users/Steve/Projects/squire-mod-v2/.planning/phases/06-work-behaviors/06-RESEARCH.md (TaskQueue dispatch code example, pitfall 7 — NBT desync)
    - src/main/java/com/sjviklabs/squire/entity/SquireEntity.java (existing addAdditionalSaveData/readAdditionalSaveData — DO NOT remove existing tags)
    - src/main/java/com/sjviklabs/squire/brain/SquireBrain.java (existing handler field declarations)
  </read_first>
  <behavior>
    TaskQueueTest (unit, write tests RED before implementing):
    - test_fifoOrder: enqueue "mine", "farm", "fish" → poll() returns "mine", then "farm", then "fish"
    - test_nbtRoundTrip: enqueue 2 tasks with params; call save(); reconstruct from load(savedTag); poll() returns identical tasks in same order
    - test_maxLength_enforcement: if MAX_QUEUE_LENGTH (16) is exceeded, enqueue returns false and queue size stays at 16
    - test_emptyPoll_returnsNull: poll() on empty queue returns null (does not throw)
    - test_clear: enqueue 3 tasks, call clear(), isEmpty() returns true

    TaskQueue implementation:
    - Backed by ArrayDeque<SquireTask> (not LinkedList as in v0.5.0)
    - MAX_QUEUE_LENGTH = 16 constant
    - enqueue(SquireTask task): returns boolean (false if at max)
    - SquireTask poll(): returns null if empty
    - boolean isEmpty()
    - void clear()
    - CompoundTag save(): serialize queue to ListTag of task CompoundTags
    - void load(CompoundTag tag): reconstruct queue from ListTag; clear() first
    - Package: com.sjviklabs.squire.brain (NOT util — per RESEARCH.md architecture section)

    SquireTask record (define in same file or as package-private class in brain/):
    - Fields: String commandName, CompoundTag params
    - toNBT() / fromNBT() for round-trip

    SquireBrain wiring (modify existing SquireBrain.java):
    - Add field: private final TaskQueue taskQueue = new TaskQueue();
    - Add accessor: public TaskQueue getTaskQueue()
    - In tick() IDLE branch (after checking currentState == IDLE): if (!taskQueue.isEmpty()) { SquireTask next = taskQueue.poll(); dispatch(next); }
    - dispatch(SquireTask task): switch on task.commandName() — route to correct handler's set-target method and forceState (use exact switch from RESEARCH.md code example)

    SquireEntity wiring (modify existing addAdditionalSaveData / readAdditionalSaveData):
    - addAdditionalSaveData: add `tag.put("taskQueue", squireBrain.getTaskQueue().save());` — do NOT remove existing save lines
    - readAdditionalSaveData: add `if (tag.contains("taskQueue")) { squireBrain.getTaskQueue().load(tag.getCompound("taskQueue")); }` — do NOT remove existing load lines
  </behavior>
  <action>
    Write TaskQueueTest first (RED). Implement TaskQueue + SquireTask. Run test (GREEN).
    Then modify SquireBrain and SquireEntity to wire the queue. These are minimal targeted additions — do not refactor existing code. Confirm ./gradlew test passes after each file.

    Critical per RESEARCH.md pitfall 7: The queue must be serialized in SquireEntity NBT methods, not in SquireBrain constructor. SquireBrain constructor creates an empty queue; SquireEntity.readAdditionalSaveData() loads it after construction.
  </action>
  <verify>
    <automated>cd C:/Users/Steve/Projects/squire-mod-v2 && ./gradlew test --tests "com.sjviklabs.squire.brain.TaskQueueTest" 2>&1 | tail -20</automated>
  </verify>
  <done>TaskQueueTest 5 assertions pass (GREEN). SquireBrain has getTaskQueue() and dispatch() wired. SquireEntity.addAdditionalSaveData/readAdditionalSaveData include taskQueue NBT hooks. ./gradlew compileJava exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: ChatHandler — personality lines and task completion messages</name>
  <files>
    src/main/java/com/sjviklabs/squire/brain/handler/ChatHandler.java
  </files>
  <read_first>
    - C:/Users/Steve/Projects/squire-mod/src/main/java/com/sjviklabs/squire/ai/handler/ChatHandler.java (v0.5.0 reference)
    - src/main/java/com/sjviklabs/squire/brain/SquireBrainEventBus.java (subscribe pattern)
    - src/main/java/com/sjviklabs/squire/config/SquireConfig.java (chatCooldownTicks, enablePersonalityChat)
  </read_first>
  <action>
    Port v0.5.0 ChatHandler. Wire in SquireBrain constructor (or an init method) via:
      bus.subscribe(SquireEvent.WORK_TASK_COMPLETE, chatHandler::onWorkComplete)
      bus.subscribe(SquireEvent.STATE_CHANGED, chatHandler::onStateChange)

    String pools per event type. Strings are in English, first-person, terse (max 1 sentence):
    - WORK_TASK_COMPLETE (generic): ["Done.", "Finished.", "Task complete, boss.", "All done here."]
    - WORK_TASK_COMPLETE "mine": ["Mined it.", "Got the block."]
    - WORK_TASK_COMPLETE "farm": ["Harvest done.", "Fields are tended."]
    - WORK_TASK_COMPLETE "fish": ["Caught something.", "Fishing done."]
    - WORK_TASK_COMPLETE "chest_deposit": ["Stowed it away.", "Chest filled."]
    - IDLE (random idle lines, fired at most once per chatCooldownTicks): ["Ready when you are.", "Standing by.", "What's next?", "Nothing to do... for now."]
    - Tier-up (LEVEL_UP event): pool indexed by new tier ordinal:
      SERVANT: "I'm getting the hang of this."
      SOLDIER: "Getting stronger."
      KNIGHT: "Knight. Has a ring to it."
      GUARDIAN: "Guardian now. I won't let you down."
      CHAMPION: "Champion. Finally."

    Spam prevention: chatLastSentTick tracked per squire; only send if (currentTick - chatLastSentTick) >= SquireConfig.chatCooldownTicks.get(). Check SquireConfig.enablePersonalityChat.get() at the top of every send method.

    Sending: squire.sendSystemMessage(Component.literal(line)) where the squire's owner is online. Get owner via squire.getOwner() → cast to ServerPlayer; if offline, skip silently.

    No unit tests required for ChatHandler — string selection is non-deterministic and behavior is verified by observation.

    After implementing ChatHandler, run ./gradlew test for full regression check.
  </action>
  <verify>
    <automated>cd C:/Users/Steve/Projects/squire-mod-v2 && ./gradlew test 2>&1 | tail -20</automated>
  </verify>
  <done>
    ChatHandler compiles. ./gradlew test exits 0 (no regressions).
    ChatHandler subscribes to WORK_TASK_COMPLETE and STATE_CHANGED events on SquireBrainEventBus.
    Spam prevention: chatCooldownTicks gate checked before every send.
    Tier-up lines exist for all 5 tiers.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Full Phase 6 implementation:
    - TorchHandler (side-effect torch placement while following)
    - MiningHandler (single block + area clear)
    - FarmingHandler (till + plant + harvest with CropBlock detection)
    - FishingHandler (vanilla loot table, correct rates)
    - PlacingHandler (single block placement from inventory)
    - ChestHandler (deposit/withdraw, vanilla + modded storage)
    - TaskQueue (FIFO, NBT-persistent, wired into SquireBrain + SquireEntity)
    - ChatHandler (personality lines, spam-gated, tier-up strings)
  </what-built>
  <how-to-verify>
    1. Run ./gradlew test — all tests green (Phase 1 + Phase 6 unit tests)
    2. Run ./gradlew compileJava — exits 0, no errors or warnings about missing methods
    3. Confirm handler package: src/main/java/com/sjviklabs/squire/brain/handler/ contains 7 handler files
    4. Confirm TaskQueue is in brain/ (not util/)
    5. Open SquireEntity.java — confirm addAdditionalSaveData and readAdditionalSaveData both reference "taskQueue"
    6. Open FishingHandler.java — confirm LootContextParamSets.FISHING is present (grep for it)
    7. Open ChestHandler.java — confirm both Capabilities.ItemHandler.BLOCK and BaseContainerBlockEntity are present
    8. Open plantable_seeds.json — confirm it exists at data/squire/tags/items/
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe which check failed</resume-signal>
</task>

</tasks>

<verification>
Final phase gate:

```bash
cd C:/Users/Steve/Projects/squire-mod-v2 && ./gradlew test
```

Expected result: All Phase 1 tests green. TaskQueueTest green (5 assertions). TorchHandlerTest green (5 assertions). MiningHandlerTest green (4 assertions). FishingHandlerTest green (3 assertions). Zero compile errors.
</verification>

<success_criteria>
- TaskQueue exists in brain/ (not util/), backed by ArrayDeque, MAX_QUEUE_LENGTH=16, NBT save/load round-trips correctly
- SquireBrain.tick() dispatches from queue on IDLE via switch on commandName
- SquireEntity NBT methods include taskQueue hooks (without removing existing save data)
- ChatHandler wired to WORK_TASK_COMPLETE and STATE_CHANGED events; all 5 tier-up lines defined
- Spam gate: enablePersonalityChat config check + cooldown ticks check before every send
- ./gradlew test exits 0
- Human verification checkpoint passes
</success_criteria>

<output>
After completion, create `.planning/phases/06-work-behaviors/06-04-SUMMARY.md` following the summary template.
</output>
