---
phase: 08-compatibility-polish
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/com/sjviklabs/squire/compat/MineColoniesCompat.java
  - src/test/java/com/sjviklabs/squire/compat/MineColoniesCompatTest.java
autonomous: true
requirements:
  - CMP-01

must_haves:
  truths:
    - "MineColoniesCompat.isColonist() returns false when MineColonies is not loaded"
    - "MineColoniesCompat.isRaider() returns false when MineColonies is not loaded"
    - "MineColoniesCompat.isFriendly() returns false for vanilla entities"
    - "isFromPackage() correctly detects com.minecolonies.core package prefix by walking class hierarchy"
    - "No MineColonies import exists anywhere in the compat class"
  artifacts:
    - path: "src/main/java/com/sjviklabs/squire/compat/MineColoniesCompat.java"
      provides: "Package-scan entity classification with ModList guard"
      exports: ["isColonist", "isRaider", "isFriendly", "isActive"]
    - path: "src/test/java/com/sjviklabs/squire/compat/MineColoniesCompatTest.java"
      provides: "Unit tests for absent-mod guard behavior"
      contains: "MineColoniesCompatTest"
  key_links:
    - from: "MineColoniesCompat.isColonist"
      to: "isFromPackage"
      via: "package prefix com.minecolonies.core.entity.citizen"
      pattern: "isFromPackage.*com\\.minecolonies\\.core\\.entity\\.citizen"
    - from: "MineColoniesCompat.isActive"
      to: "ModList.get().isLoaded"
      via: "lazy-cached boolean"
      pattern: "ModList\\.get\\(\\)\\.isLoaded.*minecolonies"
---

<objective>
Implement MineColoniesCompat — the zero-import package-scan class that prevents the squire from
attacking colonists and interfering with MineColonies AI.

Purpose: ATM10 runs MineColonies. Without this guard, the squire's CombatHandler will target
colonists as hostile mobs. MobCategory.MISC (already set in Phase 1) prevents spawn-cap
interference; this class prevents friendly-fire at the target-selection level.

Output: MineColoniesCompat.java with isColonist/isRaider/isFriendly methods + unit tests
confirming the absent-mod path returns false for all guard methods.
</objective>

<execution_context>
@C:/Users/Steve/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Steve/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/Steve/Projects/squire-mod-v2/.planning/PROJECT.md
@C:/Users/Steve/Projects/squire-mod-v2/.planning/ROADMAP.md

@C:/Users/Steve/Projects/squire-mod-v2/src/main/java/com/sjviklabs/squire/SquireMod.java
@C:/Users/Steve/Projects/squire-mod-v2/src/main/java/com/sjviklabs/squire/entity/SquireEntity.java

<interfaces>
<!-- Existing types the executor needs. No exploration required. -->

From src/main/java/com/sjviklabs/squire/entity/SquireEntity.java:
- extends PathfinderMob
- MobCategory.MISC confirmed in SquireRegistry (line 55)
- getTier() returns SquireTier

From src/test/java/com/sjviklabs/squire/inventory/SquireItemHandlerTest.java (test pattern):
- @Test annotation (JUnit 5)
- Assertions.assertEquals / assertTrue pattern established

NeoForge API:
- ModList.get().isLoaded(String modid) — safe to call after FML mod loading completes
- net.neoforged.fml.ModList (import path)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing MineColoniesCompatTest</name>
  <files>src/test/java/com/sjviklabs/squire/compat/MineColoniesCompatTest.java</files>
  <read_first>
    - src/test/java/com/sjviklabs/squire/inventory/SquireItemHandlerTest.java (test class pattern, package declarations)
    - src/main/java/com/sjviklabs/squire/SquireMod.java (modid constant = "squire")
  </read_first>
  <behavior>
    Test 1: isActive() — MineColonies is NOT in the test classpath → must return false
    Test 2: isColonist(new Pig(...)) — non-colonist entity → must return false (also exercises absent-mod path)
    Test 3: isRaider(new Pig(...)) — non-raider entity → must return false
    Test 4: isFriendly(new Pig(...)) — non-friendly entity → must return false
    Test 5: isFromPackage via reflection — create a fake class with com.minecolonies prefix in name → must return true
      (use a dynamic proxy or anonymous class with a custom classloader — OR simply test the inverse: a plain Object returns false)
    Note: MineColonies is NOT on the test classpath. isActive() will return false for all tests.
    Use HeadlessException-safe approach — no Minecraft bootstrap needed for pure Java logic tests.
  </behavior>
  <action>
    Create `src/test/java/com/sjviklabs/squire/compat/MineColoniesCompatTest.java`.

    Package: `com.sjviklabs.squire.compat`
    Imports: JUnit 5 (@Test, Assertions), java.lang.reflect for package-scan test if needed.

    Test class structure:
    ```java
    class MineColoniesCompatTest {
        @Test void isActive_returnsFalse_whenModAbsent() { ... }
        @Test void isColonist_returnsFalse_whenModAbsent() { ... }
        @Test void isRaider_returnsFalse_whenModAbsent() { ... }
        @Test void isFriendly_returnsFalse_whenModAbsent() { ... }
        @Test void isFromPackage_returnsFalse_forJavaLangObject() { ... }
    }
    ```

    The isFromPackage tests: call MineColoniesCompat's internal logic indirectly via isColonist/isRaider
    with a plain Object stand-in — since isActive() returns false when MineColonies is absent, all
    classification methods short-circuit. For testing the hierarchy walk itself, use a nested static
    class whose getName() would not match any minecolonies prefix.

    Run `./gradlew test` — tests MUST FAIL (MineColoniesCompat.java does not exist yet).
    Commit: `test(08-01): add failing MineColoniesCompatTest`
  </action>
  <verify>
    <automated>cd C:/Users/Steve/Projects/squire-mod-v2 && ./gradlew test --tests "*.MineColoniesCompatTest" 2>&1 | tail -20</automated>
  </verify>
  <done>Test file exists, compiles, tests fail with ClassNotFoundException or NoClassDefFoundError for MineColoniesCompat (not test errors)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement MineColoniesCompat and make tests green</name>
  <files>src/main/java/com/sjviklabs/squire/compat/MineColoniesCompat.java</files>
  <read_first>
    - C:/Users/Steve/Projects/squire-mod/src/main/java/com/sjviklabs/squire/compat/MineColoniesCompat.java (v0.5.0 reference — read-only)
    - src/test/java/com/sjviklabs/squire/compat/MineColoniesCompatTest.java (tests to satisfy)
    - .planning/phases/08-compatibility-polish/08-RESEARCH.md (Pattern 2: Package-Scan Detection, confirmed code example)
  </read_first>
  <behavior>
    - isActive() caches result of ModList.get().isLoaded("minecolonies") in static Boolean field
    - isColonist(Entity) returns false if !isActive(); otherwise calls isFromPackage with "com.minecolonies.core.entity.citizen"
    - isRaider(Entity) returns false if !isActive(); otherwise calls isFromPackage with "com.minecolonies.core.entity.mobs"
    - isFriendly(Entity) delegates to isColonist(entity) — citizens only (raiders should be attacked)
    - isFromPackage(Object, String) walks class hierarchy via getSuperclass() until null or Object.class; returns true on prefix match
    - ZERO MineColonies imports — no `import com.minecolonies.*` anywhere in this file
    - Class declared `public final class MineColoniesCompat` with private constructor
  </behavior>
  <action>
    Create `src/main/java/com/sjviklabs/squire/compat/MineColoniesCompat.java`.

    Package: `com.sjviklabs.squire.compat`
    Imports: net.neoforged.fml.ModList, net.minecraft.world.entity.Entity — NO minecolonies imports.

    Implement exactly per the confirmed code example in 08-RESEARCH.md (Pattern 2).
    Key points:
    - `private static Boolean modPresent = null;` (lazy init, not static initializer)
    - isActive() null-checks before calling ModList.get().isLoaded() — safe at any point after FML setup
    - isRaider checks "com.minecolonies.core.entity.mobs" prefix (raiders should still be attacked,
      so isFriendly does NOT include isRaider — only isColonist)
    - Private constructor: `private MineColoniesCompat() {}`

    Run `./gradlew test --tests "*.MineColoniesCompatTest"` — all 5 tests MUST PASS.
    Commit: `feat(08-01): implement MineColoniesCompat with package-scan guard`
  </action>
  <verify>
    <automated>cd C:/Users/Steve/Projects/squire-mod-v2 && ./gradlew test --tests "*.MineColoniesCompatTest" 2>&1 | grep -E "BUILD|PASSED|FAILED|tests"</automated>
  </verify>
  <done>
    - All 5 MineColoniesCompatTest tests pass
    - `grep -r "import com.minecolonies" src/main/java/com/sjviklabs/squire/compat/MineColoniesCompat.java` returns empty (no minecolonies imports)
    - `./gradlew test` BUILD SUCCESSFUL
  </done>
</task>

</tasks>

<verification>
./gradlew test --tests "*.MineColoniesCompatTest" — all tests green
grep -r "import com.minecolonies" src/main/java/com/sjviklabs/squire/compat/ — empty (zero MC imports)
grep "ModList.get().isLoaded" src/main/java/com/sjviklabs/squire/compat/MineColoniesCompat.java — present
grep "modPresent = null" src/main/java/com/sjviklabs/squire/compat/MineColoniesCompat.java — lazy init present
</verification>

<success_criteria>
- MineColoniesCompat.java exists in src/main/java/com/sjviklabs/squire/compat/
- Zero MineColonies API imports in the file
- isActive() lazily caches ModList result
- isColonist/isRaider/isFriendly return false when mod absent
- All 5 unit tests pass
- ./gradlew test BUILD SUCCESSFUL
</success_criteria>

<output>
After completion, create `.planning/phases/08-compatibility-polish/08-01-SUMMARY.md`
</output>
