---
phase: 08-compatibility-polish
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/com/sjviklabs/squire/compat/JadeCompat.java
  - src/main/java/com/sjviklabs/squire/compat/CuriosCompat.java
  - src/main/resources/data/squire/curios/slots/squire_accessory.json
  - src/main/resources/data/squire/curios/entities/squire_entity.json
  - src/main/resources/squire/textures/slot/squire_accessory.png
  - src/test/java/com/sjviklabs/squire/compat/CuriosCompatTest.java
autonomous: true
requirements:
  - CMP-02
  - CMP-03
  - INV-05

must_haves:
  truths:
    - "JadeCompat is a real @WailaPlugin — not a data formatting helper"
    - "Jade tooltip shows squire name, tier, and HP when Jade is present"
    - "JadeCompat does not reference SquireMod or any commonly-loaded class in static context"
    - "CuriosCompat.getHandler() returns Optional.empty() when Curios is not loaded"
    - "Curios slot JSON files are in the builtin datapack — not world-level datapack"
    - "Entity assignment JSON uses registry name squire:squire"
    - "Mod loads cleanly without Curios installed (CuriosCompatTest green)"
  artifacts:
    - path: "src/main/java/com/sjviklabs/squire/compat/JadeCompat.java"
      provides: "@WailaPlugin registration + IEntityComponentProvider tooltip"
      contains: "@WailaPlugin"
    - path: "src/main/java/com/sjviklabs/squire/compat/CuriosCompat.java"
      provides: "Runtime Curios inventory accessor with ModList guard"
      exports: ["isActive", "getHandler"]
    - path: "src/main/resources/data/squire/curios/slots/squire_accessory.json"
      provides: "Curios slot type definition (2 slots, order 200)"
      contains: "squire_accessory"
    - path: "src/main/resources/data/squire/curios/entities/squire_entity.json"
      provides: "Assigns squire_accessory slot to squire:squire entity"
      contains: "squire:squire"
    - path: "src/test/java/com/sjviklabs/squire/compat/CuriosCompatTest.java"
      provides: "Unit tests for absent-Curios guard"
      contains: "CuriosCompatTest"
  key_links:
    - from: "JadeCompat (IWailaPlugin)"
      to: "SquireTooltipProvider (IEntityComponentProvider)"
      via: "registration.registerEntityComponent(SquireTooltipProvider.INSTANCE, SquireEntity.class)"
      pattern: "registerEntityComponent.*SquireTooltipProvider"
    - from: "CuriosCompat.getHandler"
      to: "ModList.get().isLoaded(\"curios\")"
      via: "isActive() lazy cache"
      pattern: "isLoaded.*curios"
    - from: "curios/entities/squire_entity.json"
      to: "curios/slots/squire_accessory.json"
      via: "slots: [\"squire_accessory\"]"
      pattern: "squire_accessory"
---

<objective>
Implement JadeCompat (real @WailaPlugin with IEntityComponentProvider tooltip) and CuriosCompat
(runtime slot accessor + builtin datapack JSON files for slot registration).

Purpose: The squire needs a WAILA overlay showing name/tier/HP in the ATM10 HUD, and Curios
equipment slots when that mod is present. Both must degrade gracefully when the optional mod is absent.

Output:
- JadeCompat.java: @WailaPlugin annotated class + SquireTooltipProvider inner class
- CuriosCompat.java: isActive()/getHandler() with ModList guard
- Two Curios JSON datapack files (slot type + entity assignment)
- Placeholder slot icon texture (16x16 PNG)
- CuriosCompatTest.java: 3 unit tests for absent-mod guard
</objective>

<execution_context>
@C:/Users/Steve/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Steve/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/Steve/Projects/squire-mod-v2/.planning/PROJECT.md
@C:/Users/Steve/Projects/squire-mod-v2/.planning/ROADMAP.md

@C:/Users/Steve/Projects/squire-mod-v2/src/main/java/com/sjviklabs/squire/entity/SquireEntity.java
@C:/Users/Steve/Projects/squire-mod-v2/src/main/java/com/sjviklabs/squire/entity/SquireTier.java

<interfaces>
<!-- Existing types from SquireEntity. Executor uses these directly. -->

From SquireEntity.java (confirmed grep output):
- getTier() — returns SquireTier enum
- getSquireMode() — returns byte
- getHealth() — inherited from LivingEntity
- getMaxHealth() — inherited from LivingEntity
- SynchedEntityData fields: SQUIRE_MODE (Byte), SQUIRE_LEVEL (Integer), SLIM_MODEL (Boolean)
- NOTE: SQUIRE_TIER is stored via getTier() which reads from NBT/attachment, not SynchedEntityData.
  For Jade tooltip: read getTier() directly (client-side entity; safe via SynchedEntityData copy).

From build.gradle (confirmed):
- compileOnly "top.theillusivec4.curios:curios-neoforge:9.5.1+1.21.1:api"
- compileOnly "maven.modrinth:jade:15.10.4+neoforge"
- Both are on compile classpath — use real imports (not reflection).

From SquireRegistry.java line 55:
- MobCategory.MISC confirmed — squire entity type registered as squire:squire

Jade 15.x API (confirmed from RESEARCH.md):
- snownee.jade.api.IWailaPlugin
- snownee.jade.api.IEntityComponentProvider
- snownee.jade.api.EntityAccessor
- snownee.jade.api.ITooltip
- snownee.jade.api.config.IPluginConfig
- snownee.jade.api.IWailaClientRegistration
- snownee.jade.addon.universal.ItemStorageProvider — NOT needed here
- Registration: registration.registerEntityComponent(provider, SquireEntity.class)

Curios 9.5.1 API (confirmed from RESEARCH.md):
- top.theillusivec4.curios.api.CuriosApi
- top.theillusivec4.curios.api.type.capability.ICuriosItemHandler
- CuriosApi.getCuriosInventory(LivingEntity) returns Optional<ICuriosItemHandler>
- ResourceLocation-keyed attribute modifiers (UUID approach removed in 9.2.0)

Pitfall 1 (from RESEARCH.md): Do NOT reference JadeCompat from SquireMod.java or any
commonly-loaded class in a static initializer. @WailaPlugin is processed by Jade's service
loader only when Jade is present — safe as long as no other class triggers JadeCompat class load.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CuriosCompatTest (failing) + CuriosCompat.java + Curios datapack JSON</name>
  <files>
    src/test/java/com/sjviklabs/squire/compat/CuriosCompatTest.java,
    src/main/java/com/sjviklabs/squire/compat/CuriosCompat.java,
    src/main/resources/data/squire/curios/slots/squire_accessory.json,
    src/main/resources/data/squire/curios/entities/squire_entity.json,
    src/main/resources/squire/textures/slot/squire_accessory.png
  </files>
  <read_first>
    - src/test/java/com/sjviklabs/squire/inventory/SquireItemHandlerTest.java (test class pattern)
    - .planning/phases/08-compatibility-polish/08-RESEARCH.md (Pattern 4: Curios Slot Integration, Pitfall 2: slot not appearing, exact JSON format)
    - src/main/resources/data/squire/ (verify existing datapack directory structure before adding curios subdir)
  </read_first>
  <behavior>
    CuriosCompatTest:
    Test 1: isActive() returns false when curios not on classpath (Curios IS on compileOnly but not in
      test runtime — ModList.get().isLoaded("curios") will return false in unit test context)
    Test 2: getHandler(mockEntity) returns Optional.empty() when isActive() is false
    Test 3: isActive() result is cached — calling it twice does not call ModList twice
      (verify by checking modPresent field via reflection, or simply assert both calls return same value)

    CuriosCompat.java:
    - isActive() lazily caches ModList.get().isLoaded("curios") in static Boolean modPresent
    - getHandler(LivingEntity) returns Optional.empty() if !isActive(); otherwise CuriosApi.getCuriosInventory(entity)
    - Private constructor, final class

    Curios JSON files (exact content from RESEARCH.md Pattern 4):
    squire_accessory.json:
    {
      "size": 2,
      "order": 200,
      "add_cosmetic": false,
      "drop_rule": "DEFAULT"
    }
    (omit "icon" field — open question 3 from RESEARCH says use default initially; real icon in 08-03)

    squire_entity.json:
    {
      "replace": false,
      "entities": ["squire:squire"],
      "slots": ["squire_accessory"]
    }

    Placeholder texture: create a 16x16 transparent PNG at
    src/main/resources/squire/textures/slot/squire_accessory.png
    (write minimal valid PNG bytes — can use a solid color 16x16 PNG)
  </behavior>
  <action>
    Step 1: Create CuriosCompatTest.java first — tests must fail until CuriosCompat exists.
    Run `./gradlew test --tests "*.CuriosCompatTest"` — confirm FAIL.
    Commit: `test(08-02): add failing CuriosCompatTest`

    Step 2: Create CuriosCompat.java.
    Package: com.sjviklabs.squire.compat
    Imports: net.neoforged.fml.ModList, net.minecraft.world.entity.LivingEntity,
             top.theillusivec4.curios.api.CuriosApi,
             top.theillusivec4.curios.api.type.capability.ICuriosItemHandler,
             java.util.Optional

    Implement isActive() with null-check lazy init and getHandler() per research pattern.

    Step 3: Create the two Curios JSON files at exact paths:
    - src/main/resources/data/squire/curios/slots/squire_accessory.json
    - src/main/resources/data/squire/curios/entities/squire_entity.json
    Create directory: src/main/resources/data/squire/curios/slots/ and .../entities/
    Use content specified in behavior section above.

    Step 4: Create placeholder PNG. Write minimal 16x16 white PNG:
    Use Java/Python to generate or copy a simple 16x16 PNG. The file must be a valid PNG — not empty.
    Simplest approach: use Python one-liner to write PNG bytes, or copy from an existing 16x16 PNG in the project.

    Run `./gradlew test --tests "*.CuriosCompatTest"` — all 3 tests MUST PASS.
    Run `./gradlew build` — BUILD SUCCESSFUL (compile check for all compat imports).
    Commit: `feat(08-02): implement CuriosCompat with datapack slot registration`
  </action>
  <verify>
    <automated>cd C:/Users/Steve/Projects/squire-mod-v2 && ./gradlew test --tests "*.CuriosCompatTest" 2>&1 | grep -E "BUILD|PASSED|FAILED|tests"</automated>
  </verify>
  <done>
    - All 3 CuriosCompatTest tests pass
    - Both JSON files exist at exact paths: data/squire/curios/slots/squire_accessory.json and data/squire/curios/entities/squire_entity.json
    - squire_entity.json contains "squire:squire" in entities array
    - Placeholder PNG exists at squire/textures/slot/squire_accessory.png
    - ./gradlew build BUILD SUCCESSFUL
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement JadeCompat (@WailaPlugin with IEntityComponentProvider)</name>
  <files>src/main/java/com/sjviklabs/squire/compat/JadeCompat.java</files>
  <read_first>
    - .planning/phases/08-compatibility-polish/08-RESEARCH.md (Pattern 3: Jade Plugin Registration, JadeCompat.java code example, Pitfall 1: ClassNotFound, Pitfall 4: appendTooltip server thread)
    - C:/Users/Steve/Projects/squire-mod/src/main/java/com/sjviklabs/squire/compat/JadeCompat.java (v0.5.0 reference — understand what NOT to replicate)
    - src/main/java/com/sjviklabs/squire/entity/SquireEntity.java (getTier(), getHealth(), getMaxHealth() — verify accessible)
    - src/main/java/com/sjviklabs/squire/entity/SquireTier.java (enum constants for tooltip display)
  </read_first>
  <action>
    Create `src/main/java/com/sjviklabs/squire/compat/JadeCompat.java`.

    This file contains TWO classes in one file (or two separate files — put both in JadeCompat.java
    as a top-level class + inner static class, OR split into JadeCompat.java and SquireTooltipProvider.java).
    Use the two-class-one-file approach to keep the compat package tidy.

    Class 1 — JadePlugin (top-level public class, rename to JadeCompat for the file):
    ```java
    package com.sjviklabs.squire.compat;

    import snownee.jade.api.IWailaPlugin;
    import snownee.jade.api.IWailaClientRegistration;
    import snownee.jade.api.WailaPlugin;
    import com.sjviklabs.squire.entity.SquireEntity;

    @WailaPlugin
    public class JadeCompat implements IWailaPlugin {
        @Override
        public void registerClient(IWailaClientRegistration registration) {
            registration.registerEntityComponent(SquireTooltipProvider.INSTANCE, SquireEntity.class);
        }
    }
    ```

    Class 2 — SquireTooltipProvider (package-private, in same file or separate file):
    Implement IEntityComponentProvider:
    - appendTooltip(ITooltip, EntityAccessor, IPluginConfig):
      * Check accessor.getEntity() instanceof SquireEntity squire; return early if not
      * Line 1: squire name — squire.getName().getString() in ChatFormatting.YELLOW
      * Line 2: "Tier: " + squire.getTier().name() in ChatFormatting.AQUA
      * Line 3: HP line — format "HP: %.0f/%.0f", getHealth(), getMaxHealth()
        Color: GREEN if health/maxHealth > 0.5f, RED otherwise
      * Do NOT call squire.getCurrentTask() — current task FSM state is not yet in SynchedEntityData
        (Phase 2 is not complete; defer task display to gap closure if needed)
    - getUid(): return ResourceLocation.fromNamespaceAndPath("squire", "squire_entity")

    CRITICAL: JadeCompat must NOT be referenced from SquireMod.java or SquireRegistry.java.
    The @WailaPlugin annotation is auto-discovered by Jade's service loader — no manual registration.
    Do not add any call to JadeCompat from any class that loads without Jade present.

    Run `./gradlew build` — BUILD SUCCESSFUL (Jade API is compileOnly, must compile clean).
    Verify no reference to JadeCompat in SquireMod.java or SquireRegistry.java.
    Commit: `feat(08-02): implement JadeCompat @WailaPlugin with squire tooltip provider`
  </action>
  <verify>
    <automated>cd C:/Users/Steve/Projects/squire-mod-v2 && ./gradlew build 2>&1 | grep -E "BUILD|error:|warning:" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - JadeCompat.java exists in src/main/java/com/sjviklabs/squire/compat/
    - @WailaPlugin annotation present: grep "@WailaPlugin" src/main/java/com/sjviklabs/squire/compat/JadeCompat.java returns a match
    - No JadeCompat reference in SquireMod.java: grep "JadeCompat" src/main/java/com/sjviklabs/squire/SquireMod.java returns empty
    - No JadeCompat reference in SquireRegistry.java: grep "JadeCompat" src/main/java/com/sjviklabs/squire/SquireRegistry.java returns empty
    - ./gradlew build BUILD SUCCESSFUL
    - getUid() returns "squire:squire_entity" ResourceLocation
  </acceptance_criteria>
  <done>
    JadeCompat compiles clean with @WailaPlugin annotation. Tooltip provider implements getUid() and
    appendTooltip() showing name, tier, and color-coded HP. No reference to JadeCompat in
    any non-compat class. ./gradlew build BUILD SUCCESSFUL.
  </done>
</task>

</tasks>

<verification>
./gradlew test — all tests including CuriosCompatTest pass
./gradlew build — BUILD SUCCESSFUL (Jade + Curios compile deps resolve)
grep "@WailaPlugin" src/main/java/com/sjviklabs/squire/compat/JadeCompat.java — match found
grep -r "import com.minecolonies" src/main/java/com/sjviklabs/squire/compat/JadeCompat.java — empty
grep "squire:squire" src/main/resources/data/squire/curios/entities/squire_entity.json — match found
ls src/main/resources/data/squire/curios/slots/squire_accessory.json — file exists
ls src/main/resources/data/squire/curios/entities/squire_entity.json — file exists
</verification>

<success_criteria>
- JadeCompat.java: @WailaPlugin registered, IEntityComponentProvider showing name/tier/HP
- CuriosCompat.java: isActive()/getHandler() with lazy-cached ModList guard
- Two Curios JSON datapack files at correct paths in builtin resources
- Placeholder texture at squire/textures/slot/squire_accessory.png
- CuriosCompatTest: 3 tests green
- ./gradlew build BUILD SUCCESSFUL with both compileOnly deps
- No compat class referenced from SquireMod or SquireRegistry
</success_criteria>

<output>
After completion, create `.planning/phases/08-compatibility-polish/08-02-SUMMARY.md`
</output>
