---
phase: 08-compatibility-polish
plan: "03"
type: execute
wave: 2
depends_on:
  - "08-01"
  - "08-02"
files_modified:
  - src/main/resources/META-INF/neoforge.mods.toml
  - src/main/java/com/sjviklabs/squire/SquireMod.java
autonomous: false
requirements:
  - CMP-01
  - CMP-02
  - CMP-03
  - INV-05

must_haves:
  truths:
    - "neoforge.mods.toml declares jade, curios, minecolonies as optional dependencies"
    - "SquireMod.java wires MineColoniesCompat registration if mod is loaded"
    - "Mod starts without Jade, Curios, or MineColonies installed — no ClassNotFoundException"
    - "Mod starts without crashing on a dedicated server (no client-only classes in server paths)"
    - "All three compat providers confirmed present by human in ATM10 environment"
  artifacts:
    - path: "src/main/resources/META-INF/neoforge.mods.toml"
      provides: "Optional dependency declarations for jade, curios, minecolonies"
      contains: "type = \"optional\""
    - path: "src/main/java/com/sjviklabs/squire/SquireMod.java"
      provides: "Conditional MineColoniesCompat wiring via ModList guard"
      contains: "MineColoniesCompat"
  key_links:
    - from: "SquireMod constructor"
      to: "MineColoniesCompat.isActive()"
      via: "if (MineColoniesCompat.isActive()) log compat active"
      pattern: "MineColoniesCompat\\.isActive"
    - from: "neoforge.mods.toml"
      to: "jade optional dep"
      via: "type = \"optional\""
      pattern: "modId = \"jade\""
---

<objective>
Wire the three compat classes into the mod entry point, declare optional dependencies in
neoforge.mods.toml, run the full test suite, and verify clean startup without any optional mod.
Then checkpoint for human ATM10 smoke test (Jade overlay, Curios slots, MineColonies coexistence).

Purpose: Compat code exists but nothing wires it in. The neoforge.mods.toml has no optional dep
declarations. Without mod descriptor entries, NeoForge may load in undefined order relative to
optional deps. The polish pass is also the final gate before this phase is complete.

Output: Updated SquireMod.java + neoforge.mods.toml + human sign-off on ATM10 behavior.
</objective>

<execution_context>
@C:/Users/Steve/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Steve/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/Steve/Projects/squire-mod-v2/.planning/PROJECT.md
@C:/Users/Steve/Projects/squire-mod-v2/.planning/ROADMAP.md

@C:/Users/Steve/Projects/squire-mod-v2/.planning/phases/08-compatibility-polish/08-01-SUMMARY.md
@C:/Users/Steve/Projects/squire-mod-v2/.planning/phases/08-compatibility-polish/08-02-SUMMARY.md

@C:/Users/Steve/Projects/squire-mod-v2/src/main/java/com/sjviklabs/squire/SquireMod.java
@C:/Users/Steve/Projects/squire-mod-v2/src/main/resources/META-INF/neoforge.mods.toml

<interfaces>
<!-- Existing types. No exploration required. -->

From SquireMod.java (current state — confirmed):
```java
@Mod(SquireMod.MODID)
public final class SquireMod {
    public static final String MODID = "squire";
    public SquireMod(IEventBus modEventBus, ModContainer container) {
        LOGGER.info("[Squire] Initializing Squire Mod v2");
        SquireRegistry.register(modEventBus);
        container.registerConfig(ModConfig.Type.COMMON, SquireConfig.SPEC, "squire-common.toml");
    }
}
```

From neoforge.mods.toml (current state — confirmed):
- Has required deps for neoforge, minecraft, geckolib
- NO jade, curios, or minecolonies entries yet

From 08-RESEARCH.md Pattern 5 — exact TOML to add:
[[dependencies.squire]]
    modId = "jade"
    type = "optional"
    versionRange = "[15.0,)"
    ordering = "NONE"
    side = "CLIENT"

[[dependencies.squire]]
    modId = "curios"
    type = "optional"
    versionRange = "[9.5,)"
    ordering = "NONE"
    side = "BOTH"

[[dependencies.squire]]
    modId = "minecolonies"
    type = "optional"
    versionRange = "[1.1,)"
    ordering = "NONE"
    side = "BOTH"

MineColoniesCompat wiring note: MineColoniesCompat is self-guarding (isActive() returns false
when mod absent). In SquireMod, just log compat status at startup — no conditional registration
needed. The friendly-fire integration point (CombatHandler) is Phase 4 work; this plan only
wires the startup logging and confirms compat is reachable.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire optional deps into neoforge.mods.toml and SquireMod.java, run full test suite</name>
  <files>
    src/main/resources/META-INF/neoforge.mods.toml,
    src/main/java/com/sjviklabs/squire/SquireMod.java
  </files>
  <read_first>
    - src/main/resources/META-INF/neoforge.mods.toml (current content — do not remove existing deps)
    - src/main/java/com/sjviklabs/squire/SquireMod.java (current constructor — add to it, do not replace)
    - src/main/java/com/sjviklabs/squire/compat/MineColoniesCompat.java (isActive() signature)
    - src/main/java/com/sjviklabs/squire/compat/CuriosCompat.java (isActive() signature)
    - .planning/phases/08-compatibility-polish/08-RESEARCH.md (Pattern 5: neoforge.mods.toml optional deps, exact TOML)
  </read_first>
  <action>
    Step 1 — neoforge.mods.toml:
    Append the three optional dependency blocks from RESEARCH.md Pattern 5 to the end of
    neoforge.mods.toml. Read the file first to confirm current content, then append.
    Do not modify existing geckolib/neoforge/minecraft dep entries.

    Step 2 — SquireMod.java:
    Add compat startup logging to the constructor body after existing calls:
    ```java
    // Compat startup logging
    if (MineColoniesCompat.isActive()) {
        LOGGER.info("[Squire] MineColonies detected — citizen friendly-fire prevention active");
    }
    if (CuriosCompat.isActive()) {
        LOGGER.info("[Squire] Curios detected — accessory slots registered via datapack");
    }
    // Jade is auto-discovered via @WailaPlugin — no logging needed here
    ```
    Add imports: com.sjviklabs.squire.compat.MineColoniesCompat, CuriosCompat.

    Step 3 — Run full test suite:
    `./gradlew test` — all unit tests must pass (MineColoniesCompatTest + CuriosCompatTest +
    existing SquireItemHandlerTest + entity tests).

    Step 4 — Build:
    `./gradlew build` — BUILD SUCCESSFUL. The mod JAR compiles with all three compat classes.

    Step 5 — Dedicated server safety check:
    Run grep to verify no client-only class references in non-@OnlyIn paths:
    `grep -r "net.minecraft.client" src/main/java/com/sjviklabs/squire/compat/` — must return empty.
    (JadeCompat uses Jade client API, but it's only loaded by Jade's service loader on CLIENT side.
    Verify JadeCompat has no @Mod.EventBusSubscriber without dist check.)

    Commit: `feat(08-03): wire optional deps in mods.toml and SquireMod startup logging`
  </action>
  <verify>
    <automated>cd C:/Users/Steve/Projects/squire-mod-v2 && ./gradlew test 2>&1 | grep -E "BUILD|tests.*passed|FAILED" && grep "type = \"optional\"" src/main/resources/META-INF/neoforge.mods.toml | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - neoforge.mods.toml contains exactly 3 optional dependency entries (jade, curios, minecolonies)
    - grep 'type = "optional"' neoforge.mods.toml returns 3 matches
    - SquireMod.java imports MineColoniesCompat and CuriosCompat
    - ./gradlew test BUILD SUCCESSFUL — all existing + new tests pass
    - ./gradlew build BUILD SUCCESSFUL
    - grep -r "net.minecraft.client" src/main/java/com/sjviklabs/squire/compat/ returns empty
  </acceptance_criteria>
  <done>
    neoforge.mods.toml has three optional dep blocks. SquireMod constructor logs compat status.
    Full test suite green. Build clean. No client-only class references in compat package server paths.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: ATM10 smoke test — verify compat in live environment</name>
  <files>none</files>
  <action>Human verification of built mod in ATM10 environment. See how-to-verify section below.</action>
  <what-built>
    Three compat providers:
    1. MineColoniesCompat — package-scan entity classification, friendly-fire gate
    2. JadeCompat — @WailaPlugin showing squire name/tier/HP in HUD overlay
    3. CuriosCompat — Curios slots registered via datapack JSON (squire_accessory, 2 slots)

    Supporting infrastructure:
    - neoforge.mods.toml optional dep declarations for all three mods
    - Builtin datapack curios JSON files
    - Unit tests: MineColoniesCompatTest (5 tests), CuriosCompatTest (3 tests)
  </what-built>
  <how-to-verify>
    Build the mod JAR first: `./gradlew build` — confirm squire-mod-v2-2.0.0.jar in build/libs/

    Test A — Absent optional mods (baseline, most critical):
    1. Copy JAR to a test instance with ONLY NeoForge 1.21.1 + Geckolib (no Jade, Curios, MineColonies)
    2. Start the server or client
    3. Confirm: server/client starts without ClassNotFoundException or NoClassDefFoundError
    4. Check log for "[Squire] Initializing" — should appear, no errors after it
    5. Confirm: no "[Squire] MineColonies detected" or "[Squire] Curios detected" in log (correct — mods absent)

    Test B — With Jade present:
    1. Add Jade 15.10.4 to the test instance
    2. Start client, spawn a squire
    3. Look at the squire — Jade HUD should show:
       - Squire's name (yellow text)
       - "Tier: SERVANT" (or current tier, aqua text)
       - "HP: X/Y" (green if >50% health, red if <=50%)
    4. Confirm overlay appears without crashing

    Test C — With Curios present:
    1. Add Curios 9.5.1 to the test instance
    2. Start client, spawn a squire, open squire inventory
    3. Confirm: "Accessories" slot section appears in the squire inventory screen with 2 slots
    4. Confirm: "[Squire] Curios detected" appears in server log

    Test D — With MineColonies present (optional — requires MineColonies in ATM10):
    1. In ATM10 with MineColonies active, spawn a squire near a colony
    2. Confirm: "[Squire] MineColonies detected" in server log
    3. Confirm: squire does NOT attack citizens (friendly-fire prevention — note: full test
       requires Phase 4 CombatHandler integration; for now verify the log message and no crash)

    ATM10 full pack test (if available):
    1. Drop the JAR into ATM10 6.2 mods folder
    2. Start ATM10 — all 449 mods loading
    3. Confirm: no squire-related errors during startup
    4. Spawn squire, look at it with Jade HUD visible — overlay shows
  </how-to-verify>
  <verify>Human confirmed via resume-signal below</verify>
  <done>Human typed "approved" confirming mod starts clean without optional deps, Jade HUD visible, Curios slots present</done>
  <resume-signal>Type "approved" with notes on any issues found, or describe failures for gap closure</resume-signal>
</task>

</tasks>

<verification>
./gradlew test — all tests pass (existing Phase 1 tests + MineColoniesCompatTest + CuriosCompatTest)
./gradlew build — BUILD SUCCESSFUL
grep 'type = "optional"' src/main/resources/META-INF/neoforge.mods.toml | wc -l → 3
grep -r "net.minecraft.client" src/main/java/com/sjviklabs/squire/compat/ → empty
Human checkpoint: ATM10 smoke test passed
</verification>

<success_criteria>
- neoforge.mods.toml: 3 optional dependency entries (jade CLIENT, curios BOTH, minecolonies BOTH)
- SquireMod.java: startup logging for MineColoniesCompat.isActive() and CuriosCompat.isActive()
- Full test suite passes: ./gradlew test BUILD SUCCESSFUL
- Mod JAR builds clean: ./gradlew build BUILD SUCCESSFUL
- Human verified: mod starts without any optional dep present (no ClassNotFoundException)
- Human verified: Jade HUD shows squire name/tier/HP when Jade is present
- Human verified: Curios accessory slots appear in squire inventory when Curios is present
</success_criteria>

<output>
After completion, create `.planning/phases/08-compatibility-polish/08-03-SUMMARY.md`
</output>
